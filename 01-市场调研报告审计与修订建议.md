# 市场调研报告审计与修订建议

> 审计对象：`创作流程市场调研报告.md`  
> 审计结论：**方向合理，可作为方案输入；证据强度和表述方式需要系统性修订，不能直接当作产品规则。**

## 1. 总体评价

报告抓住了内容创作工具的主要矛盾：创作者需要的不是“一键代写”，而是从模糊素材到可发布内容的低摩擦过程。报告提出的“选题—结构—初稿—编辑/发布”漏斗，以及 AI 生成后由人确认的思路，适合本项目；但“结构”和“初稿”不能默认等同于一份通用长文。共享阶段应止于选题和核心内容骨架，文章大纲、图文分镜与完整成稿应在平台分流后分别定义。

问题主要不在产品方向，而在证据管理：部分数字来自二手文章、经验总结或未来预测，却被写成确定结论；部分第三方工具能力又被等同为平台正式能力。这会导致产品过度校验、发布风险误判和研发优先级偏差。

## 2. 可以直接保留的结论

| 结论 | 判断 | 证据等级 | 产品落地 |
|---|---|---:|---|
| AI 生成后必须允许人工编辑和确认 | 合理 | B | 所有生成步骤均保存版本，发布前强制确认 |
| 先确认选题和核心内容，再生成平台成稿 | 合理 | B | 采用 Topic Brief → ContentCore → 平台原生大纲/分镜与成稿 |
| 音频在记录后先做 ASR | 合理 | B | 异步转写、时间戳、可纠错 |
| 原始图片应保留并优先复用 | 合理 | B | 原图是资产和事实证据，不默认重绘 |
| 小红书与公众号应分别完成原生创作和排版 | 合理 | A/B | 共用事实与观点核心，分别生成平台稿，再进入渲染/发布适配 |
| 公众号优先进入草稿箱而非直接群发 | 合理 | A/B | 官方 API 连接后创建草稿，用户在平台复核 |
| Skill 负责实时数据获取，LLM 负责理解和生成 | 合理 | B | 热点适配层与 LLM 层分离 |
| 发布动作应保留人工触发 | 合理 | B | 防止误发、重复发和账号风险 |

## 3. 必须修订的内容

### 3.1 平台经验不能写成硬规则

以下类型的结论可以作为默认建议或实验变量，但不能成为发布阻断条件：

- “小红书 4–9 张图片最佳”
- “少于 3 张会被判定单薄”
- “正文应为 300–800 字”
- “某固定时间段一定更好”
- “标题必须控制在某经验长度”

修订方式：

```text
错误：图片少于 3 张，不允许发布。
建议：当前为 2 张图。知识清单类内容通常可尝试增加步骤图或总结图；仍可继续发布。
```

系统应区分：

- **平台技术限制**：阻断；来自实际接口返回或当前官方文档。
- **账号历史表现**：提醒；来自该用户自己的数据。
- **行业经验建议**：弱提醒；允许忽略。
- **实验建议**：用于 A/B 测试，不在编辑器中制造焦虑。

### 3.2 不应固化 Cookie 与风控数字

报告中关于 Cookie 固定存活天数、同一账号只能一个 Web Session、固定发布间隔、违规固定封禁天数等描述，缺乏足够可靠的一手依据，而且平台机制会动态变化。

修订为：

- 登录态属于不可预测的外部状态；每次发布前检查。
- 发布桥返回明确错误码和截图/诊断信息。
- 禁止系统通过随机等待等手段刻意规避平台风控。
- 账号状态异常时停止自动重试，交给用户处理。
- 发布任务必须幂等，防止失败重试造成重复笔记。

### 3.3 “无官方 API”需要更严谨

更可靠的表述是：

> 截至本方案核验时，未发现面向普通个人创作者公开、稳定、通用的小红书笔记发布 API。本项目拟使用的 `xiaohongshu-mcp` 是第三方浏览器自动化项目，其技术可用性不代表平台官方授权。

因此，第三方通路必须：

- 默认本地执行；
- 不在云端保存 Cookie；
- 在 UI 中明确标记为“实验性连接”；
- 发布前展示完整 payload 并要求确认；
- 提供“仅导出发布包”的安全退路。

### 3.4 市场规模与未来预测不应驱动 MVP

报告中的市场规模、复合增长率、未来年份预测等，即使来源存在，也无法直接证明目标用户会为本产品付费。MVP 应由下面的问题驱动：

1. 创作者收集的灵感是否真的经常无法转化成内容？
2. 日期白板是否比文件夹、标签、搜索更容易回忆？
3. 从灵感到选题最费力的是关联、角度还是素材整理？
4. 双平台用户是希望“一稿两发”，还是希望保留明显不同的人设和表达？
5. 用户是否愿意让第三方工具执行小红书发布，还是只愿导出？

## 4. 原报告未充分覆盖的关键问题

### 4.1 缺少“共享内容核心”设计

多模态素材进入 LLM 后，如果没有原件、转写、OCR、图片描述和引用关系，生成内容难以追溯。需增加统一的 Evidence/Asset 模型，并在选题确认后形成 `ContentCore`。它保存主题、受众、读者问题、核心承诺、主张、事实、证据、案例、素材、待核验项以及必须包含/避免的内容，但不保存完整公众号文章、小红书正文、平台标题、图文分镜、CTA 或标签。

这使两个平台共享“内容为什么成立”，而不是共享“最终应该长什么样”。

### 4.2 缺少平台分流与版本同步策略

小红书和公众号如果围绕同一主题创作，应引用同一个 `ContentCore`，但各自拥有独立平台稿：

- 小红书稿包含笔记策略、标题/钩子、图文分镜、卡片文案、正文和标签；
- 公众号稿包含文章策略、标题、文章大纲、分节成稿、图片位置和 CTA；
- 每个平台稿记录 `contentCoreId`、`sourceCoreVersion`、`syncStatus`、`userLockedPaths` 和生成时间；
- `ContentCore` 更新后，关联平台稿标记为 `stale` 并展示差异；
- 系统不得静默覆盖平台人工修改；
- MVP 使用“重新生成新版本 + 差异预览 + 用户选择”，块级三方合并延后到 P2。

完整“母稿”仅作为已有文章导入或专业用户的可选模式，不作为默认必经节点。

### 4.3 缺少热点的证据与过期机制

热点不能只保存一段 AI 摘要，还要保存：

- 来源平台和原 URL；
- 抓取时间与源发布时间；
- 原始快照或内容哈希；
- 热度证据；
- 过期状态；
- 是否被多个来源交叉印证。

### 4.4 缺少发布失败恢复与重复发布防护

发布不是按钮点击成功即可。系统需要 `publication_job` 状态机、幂等键、外部 ID、重试策略和人工恢复。

## 5. 修订后的研究结论

### 已证实或高可信

- 创作流程应分阶段，每一阶段允许人修改。
- 多模态素材需要先结构化，再进入创作。
- 同一核心内容骨架应生成结构明显不同、但事实可追溯的两份平台原生稿。
- 公众号官方草稿箱是较可控的发布目标。
- Code-2 的结构化中间层、卡片预览和导出能力可复用。
- `xiaohongshu-mcp` 具备登录、发布图文/视频等工程能力，但属于第三方执行通道。
- AIHot skill 的公开契约说明其通过公开 API 获取实时热点，可作为热点源适配器。

### 需要验证的产品假设

- 用户是否喜欢按日期寻找灵感，还是更多依赖搜索和标签。
- “自由但有秩序”的白板是否会提升记录意愿。
- 相关性分数是否帮助决策，还是让用户觉得 AI 在阻碍创意。
- 用户希望 AI 生成多少图片、什么类型的图片。
- 双平台用户是否理解并接受“共享核心内容、平台分流后分别成稿”的模式。
- 小红书本地发布桥的安装成本是否可接受。

### 上线前必须重新核验

- 微信公众号账号类型和当前接口权限。
- 草稿、素材、图片上传接口的最新字段和限制。
- AIHot 服务的稳定性、限流、使用条款和数据来源。
- `xiaohongshu-mcp` 最新版本、登录方式和异常行为。
- 小红书平台当前规则及用户账号风险承受度。

## 6. 建议的证据治理方式

为每条产品规则建立配置记录：

```yaml
rule_id: xhs.image_count.suggestion
kind: recommendation       # technical_limit | recommendation | experiment
source: account_analytics  # official | runtime_probe | account_analytics | research
confidence: medium
updated_at: 2026-08-17
blocking: false
message: 建议补充步骤图或总结图，但可继续发布
```

这样平台规则变化时不需要改写核心流程，也不会把经验建议伪装成平台限制。

## 7. 结论

原报告可以保留作为“问题与方向研究”，但不宜直接作为 PRD 或接口规范。修订后应以以下顺序驱动研发：

1. 用户任务与内容状态机；
2. 统一素材与证据模型；
3. 核心内容骨架、双平台原生创作与版本同步；
4. 平台渲染/发布适配器和连接能力探测；
5. 可配置建议；
6. 用户研究和真实数据回流。

下一步产品定义见：[02-产品需求与信息架构方案.md](./02-产品需求与信息架构方案.md)。

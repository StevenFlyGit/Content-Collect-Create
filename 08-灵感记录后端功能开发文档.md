# 灵感记录板块后端功能开发文档

> 适用板块：AI 内容工作站「灵感记录 / 灵感库」（对应 `Nebula-React` 前端：`/capture` 记录页、`/timeline` 灵感库、`/` 首页最近灵感）。
> 技术栈基线：前端 React 18 + Vite 5（`Nebula-React`，路由 `/`、`/capture`、`/timeline`）；后端 Node + Express（与项目既有 `Code-8` 一致）；数据层 **阿里云 RDS PostgreSQL Serverless** + **对象存储 OSS**。
> 范围边界：**不含** OCR / ASR / AI 标签自动融合。本文档不依赖任务队列、Worker、AI 网关。
> 文档状态说明（2026-08-20）：本文同时记录**目标方案**与**当前实现差距**。目标方案要求灵感类型完整 CRUD、后端随机配色，以及灵感内容创建、二次展示、详情、编辑和删除闭环；当前代码中灵感内容详情/更新/软删除后端接口已存在，但类型后端仍只有列表与新增，前端仍缺少完整的类型管理和灵感详情/编辑/删除交互。除明确标注“当前已实现”的内容外，不应据本文认定代码已经完成。

---

## 0. 评估结论

**结论：RDS PostgreSQL Serverless + OSS 作为数据层可行，但不能单独构成完整后端；必须配套 Application API/Express 运行时、鉴权、幂等、上传校验和一致性补偿机制。若方案被理解为“只采购 RDS + OSS、没有业务 API”，则不可行。**

理由：

1. **本板块本质是结构化 CRUD + 文件对象存储 + 按日期/关键词检索**，RDS + OSS 足以覆盖数据层，不需要为本范围额外引入队列、Worker 或 AI 服务；但仍必须有 Application API 承载业务逻辑。
2. **被明确排除的能力，恰好是唯一需要 Worker / 任务队列 / AI 网关的重活**：OCR（图片转文字）、ASR（音频转写）、AI 标签融合，都属于「慢、需重试、需调外部 AI」的异步处理。它们不在范围内，因此任务队列与 Worker 也无需在本板块引入（后续「创作 / 发布」板块再补）。
3. **应用 API/计算运行时不可省略，且当前实现仍有缺口**：项目已经存在 Express 服务及部分 RDS/OSS 接口，但这不代表前后端闭环已经完整。类型详情、编辑、删除接口，后端随机配色，以及前端类型管理与灵感详情/编辑/删除交互仍需按本文目标补齐。Express 仍需部署在可运行、可监控的环境中；RDS 与 OSS 不会自动提供这些业务能力。

需要补充的「能力」（均为应用层代码，非独立服务）：

| 需补充项 | 形态 | 是否独立服务 |
|---|---|---|
| 应用 API（Express 服务） | 代码，与前端同仓库或独立部署 | 否 |
| 对象存储访问（OSS SDK + 预签名） | 代码 + 阿里云 OSS 桶 | 否（用现成 OSS） |
| 租户 / 鉴权（workspace 隔离） | 代码 + RDS 表 | 否（落在 RDS 内） |
| 关系型存储（RDS PostgreSQL Serverless） | 阿里云资源 | 是（用户已指定） |

**不在本板块、但需在架构中预留接口位（未实现不影响本板块上线）：**
- `asset_derivatives`（OCR/transcript/caption）表——本期不是必需项；可仅保留未来迁移设计，不应把空表误认为 OCR/ASR 能力已经存在；
- 任务队列 / Worker——本板块无异步重活，不引入。

---

## 1. 现有前端与后端能力盘点（对齐基线）

字段、接口与交互设计必须同时对齐现有页面和当前代码事实，避免把目标方案误写为已实现：

| 位置 | 当前能力 / 数据 | 目标对齐点与当前差距 |
|---|---|---|
| `CapturePage.jsx` | 标题、正文、类型、附件、自动保存、`保存草稿` / `提交灵感`、发生时间 | 继续复用 IndexedDB 草稿与远端提交语义；创建自定义类型时当前仍固定提交 `color_token: '--ink-muted'`，必须改为前端只提交 `label` 与可选 `icon`，由后端随机配色 |
| `TypeSelect.jsx` | 展示 7 个系统初始类型，并可新增自定义类型 | 目标为类型查询、详情、新增、编辑、删除/归档完整闭环；新增后使用 API 返回的稳定 `color_token` 渲染圆圈 |
| `server/src/routes/types.js` | 当前只有 `GET /api/inspiration-types` 与 `POST /api/inspiration-types` | 当前缺少类型详情、编辑、删除接口；新增接口当前接收客户端颜色，也未执行六色随机规则，属于明确实现缺口 |
| `TimelinePage.jsx` | 日期白板、贴片、星云视图、多选及搜索/筛选入口 | 已提交灵感需支持摘要二次展示、点击详情、编辑、删除；更新或删除成功后同步刷新首页、白板、灵感库和搜索结果 |
| `HomePage.jsx` | 最近灵感与当日计数 | 接入真实列表/计数接口，并在编辑、删除后失效缓存或重新查询 |
| `server/src/routes/inspirations.js` | 已有创建、列表、搜索、详情、更新和软删除接口 | 后端内容 CRUD 基础已存在；删除返回 `202` 并进入附件删除补偿流程。仍需以前端完整交互和契约测试验证可用性 |
| `inspirations.js`（mock） | `id, type, time, pos, text, attachments, aiTag, used, quote` | 逐步替换为真实 API 字段；`aiTag` 本期为空，不得暗示 AI 标签融合已实现 |
| `03 §5.1` | 未完成上传可存本地草稿，但不能误报已同步 | 只有 IndexedDB 事务成功才显示“草稿已保存”；只有 RDS/OSS 完整流程成功才清理本地草稿 |
| `03 §5.2` | 每个附件独立状态 | `assets.status` 独立驱动；图片最大 20MB，录音/音频最长 60 秒 |
| `03 §7.1` | 白板有限自由布局 | 使用稳定灵感 ID 与 `daily_boards` / `board_position_json` 持久化 |

## 2. 整体架构设计

### 2.1 分层（对齐 `06 §1`，仅保留本板块所需）

```
┌─────────────────────────────────────────────────────────┐
│  Web 工作台（Nebula-React · React/Vite）                  │
│  /capture 记录  ·  /timeline 灵感库  ·  / 首页最近灵感     │
└───────────────────────────┬─────────────────────────────┘
                            │  HTTPS REST（fetch）
┌───────────────────────────▼─────────────────────────────┐
│  Application API（Node + Express）                        │
│  · 鉴权 / workspace 隔离                                   │
│  · 灵感 CRUD、列表、检索、筛选                             │
│  · OSS 预签名签发（presign）/ 收尾（complete）             │
│  · 白板布局读写                                            │
│  （本板块内联处理，无任务队列 / Worker）                    │
└───────┬───────────────────────────────┬──────────────────┘
        │  SQL                          │  SDK（短期签名 URL）
┌───────▼────────────┐        ┌──────────▼────────────────┐
│  RDS PostgreSQL     │        │  对象存储 OSS              │
│  Serverless         │        │  · 图片 / 音频 / 文件字节   │
│  · 结构化元数据      │        │  · 缩略图（可选，前端裁切） │
│  · workspace 隔离    │        │  · 命名空间 {ws}/{uuid}     │
└─────────────────────┘        └───────────────────────────┘
```

### 2.2 明确不在本板块范围（不实现）

- 任务队列 / Worker（无 OCR/ASR，无慢任务）
- AI 网关 / LLM 调用（无 AI 标签融合）
- `asset_derivatives` 写入（表先建，留空）

### 2.3 技术栈与代码组织（对齐现有工程）

后端建议独立目录 `Nebula-React/server/`（或独立仓库 `nebula-api`），与前端 `Nebula-React` 同工程、各自构建：

```
server/
├─ src/
│  ├─ index.js            # Express 启动、挂载路由
│  ├─ db.js               # pg 连接池（RDS Serverless 端点）
│  ├─ oss.js              # 阿里云 OSS client + presign 封装
│  ├─ auth.js             # workspace 鉴权中间件（Bearer / Session）
│  ├─ routes/
│  │  ├─ inspirations.js  # 灵感 CRUD / 列表 / 检索
│  │  ├─ assets.js        # 预签名 / complete
│  │  ├─ types.js         # 灵感类型
│  │  └─ boards.js        # 白板布局
│  └─ validators.js       # 入参校验（mime/大小/类型枚举）
├─ migrations/            # SQL 建表（见 §4）
└─ package.json
```

依赖：`express`、`pg`（node-postgres）、`ali-oss`（或 `@aws-sdk/client-s3` 兼容模式）、`zod`（入参校验）、`cors`。前端通过 `vite` 代理将 `/api` 转发到 `http://127.0.0.1:32xx`（与 `Code-8` 监听习惯一致），生产由网关统一入口。

---

## 3. 数据存储方案：RDS 与 OSS 分工

**原则（来自 `06 §3.2`）：字节只存在于 OSS；数据库只存元数据与指针（`storage_key`）。**

| 存什么 | 载体 | 说明 |
|---|---|---|
| 图片 / 音频 / 文件原始字节 | **OSS** | 直传，不经应用服务器带宽 |
| 缩略图（可选） | OSS（派生，可前端裁切后传，或后端用 OSS 图片处理） | 不在本板块强制；若做，独立 object |
| 灵感正文、标题、类型、发生时间、标签、布局 | **RDS** | 结构化、可检索、可聚合 |
| 附件元数据（mime、大小、时长/尺寸、状态） | **RDS `assets`** | 指向 OSS 的 `storage_key` |
| 派生结果（OCR/transcript/caption） | RDS `asset_derivatives`（**暂空**） | 待 AI 板块 |

**禁止**：把文件 base64 写入 RDS（录音尤甚，体积爆炸）；把字节存数据库大字段。

**OSS 命名空间**：`{workspace_id}/{uuid}.{ext}`，与 `assets.storage_key` 一一对应。CORS 允许浏览器直传 PUT（或改服务端代理上传，见 §9）。

---

## 4. 核心数据结构与表结构设计

基于 `06 §3.2 / §3.3` 的逻辑模型，裁剪到「灵感记录」板块所需表，并补齐约束与索引。

### 4.1 `workspaces`（租户根，精简）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | uuid | PK, default gen_random_uuid() | 工作区 |
| `owner_id` | uuid | NOT NULL | 归属用户（对接统一账号体系） |
| `name` | varchar(80) | NOT NULL | 显示名 |
| `created_at` | timestamptz | NOT NULL default now() | |

索引：`PK(id)`。

### 4.2 `inspiration_types`（系统初始类型 + 工作区自定义类型）

术语统一为“系统初始类型”和“工作区自定义类型”。“自定义标签”是另一项多选标签能力，不得与自定义类型混用。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | uuid | PK | 类型稳定标识 |
| `workspace_id` | uuid | FK→workspaces(id), nullable | 系统初始类型为 `NULL`；工作区自定义类型为当前工作区 ID |
| `slug` | varchar(40) | NOT NULL | 稳定机器键；自定义类型由后端生成，重命名时不变化 |
| `label` | varchar(40) | NOT NULL | 用户可见名称 |
| `color_token` | varchar(40) | NOT NULL | 类型圆圈、标签和卡片强调色；创建后持久化且保持稳定 |
| `icon` | varchar(40) | nullable | 可选图标 |
| `sort_order` | int | NOT NULL default 0 | 左下角类型选择列表排序 |
| `archived_at` | timestamptz | nullable | 自定义类型删除/停用时间；`NULL` 表示活动 |

API 可派生返回 `is_system = (workspace_id IS NULL)`，无需额外持久化该字段。活动类型名称在“系统初始类型 + 当前工作区未归档自定义类型”范围内不可重复；`slug` 在各自作用域内唯一。

系统初始类型及颜色：

```text
先不分类 -> --ink-muted
想法     -> --nebula-violet
引用     -> --nebula-blue
随感     -> --nebula-rose
待办     -> --nebula-mint
案例     -> --nebula-amber
问题     -> --danger
```

**自定义类型随机配色规则：**

- 新建工作区自定义类型时，前端只提交 `label` 与可选 `icon`，不得提交或指定最终 `color_token`。
- 后端从以下六色白名单中随机选择一个，并与新类型在同一次数据库写入中持久化：`--nebula-violet`、`--nebula-blue`、`--nebula-rose`、`--nebula-mint`、`--nebula-amber`、`--danger`。
- `--ink-muted` 仅用于系统初始类型“先不分类”，不属于自定义类型随机池；自定义类型不得固定显示为灰色。
- 本期不要求六色均匀分布，也不要求相邻类型不重复；但每次创建的返回值必须命中白名单。
- 颜色一经分配，刷新、重新查询、服务重启和类型重命名时均不得重新随机；本期编辑接口不开放改色。

**编辑、删除与重新启用语义：**

- 当前类型管理界面就是快速记录面板左下角展开的类型选择列表，不新增独立类型管理页面、管理抽屉或额外“管理类型”入口。
- 系统初始类型仅可查询和查看详情，不允许工作区编辑或删除；左下角选项右侧不显示删除叉号。
- 工作区自定义类型支持查看详情、编辑 `label` / `icon` / `sort_order`，以及删除/归档；活动自定义类型名称右侧显示小型叉号作为停用入口。
- 删除采用软删除：`archived_at = now()`，不物理删除类型行，不级联删除已有灵感。
- 已归档类型从新建/编辑灵感的普通选择器中排除；历史灵感继续返回并显示原类型名称和颜色，可标记“类型已停用”。编辑历史灵感时可保留该类型，也可改为活动类型；一旦换走，不允许重新选择已归档类型。
- “重新添加已删除类型即重新启用”：`POST /api/inspiration-types` 对 `label` 做项目既有的裁剪与名称规范化后，如果命中当前工作区已归档的同名类型，应更新原记录为 `archived_at = NULL`，不插入新行。重新启用必须保留原 `id`、`slug`、`color_token` 及其他稳定身份字段，不重新随机颜色。
- 活动同名类型仍返回名称冲突；只有当前工作区已归档同名类型进入重新启用分支。实现时应在同一数据库事务内锁定候选记录或依赖唯一约束，避免并发请求同时恢复/创建而产生重复类型。

### 4.3 `inspirations`（灵感主表）

对齐 `inspirations.js` 字段 + `03 §5.1` 草稿态 + `06 §3.2`。

| 字段 | 类型 | 约束 | 索引/说明 |
|---|---|---|---|
| `id` | uuid | PK | |
| `workspace_id` | uuid | FK→workspaces(id) NOT NULL | `idx_insp_ws (workspace_id)` |
| `recorded_at` | timestamptz | NOT NULL | 灵感**发生**时间（来自前端 `date-stamp`，默认 now()）；决定归属日期 |
| `title` | varchar(200) | nullable | 标题可空（前端 placeholder「给这一刻起个名字（可留空）」） |
| `text_raw` | text | NOT NULL | 正文（`body-input`） |
| `text_normalized` | text | nullable | 归一化副本（检索用，可空） |
| `type_id` | uuid | FK→inspiration_types(id) nullable | 类型；`先不分类` 即 NULL |
| `user_tags_json` | jsonb | default '[]' | 用户标签（前端「标签」位，暂未实装 UI，预留） |
| `ai_tags_json` | jsonb | default '[]' | **AI 标签，本板块留空**，待融合上线 |
| `board_position_json` | jsonb | nullable | 白板贴片坐标 `{x,y,z,rotation,col}`（`03 §7.1`），也可集中存 `daily_boards` |
| `processing_status` | varchar(20) | NOT NULL default 'draft' | 见 §8.2 状态机：`draft` / `synced` / `failed` |
| `sync_status` | varchar(20) | NOT NULL default 'local' | `local`（仅本地草稿，未同步）/ `synced`（已落库） |
| `is_quote` | boolean | default false | 对应 mock `quote:true`（引用类标识，可冗余 type 也可用） |
| `used_in_projects` | int | default 0 | 对应 mock `used: '用于 N 个项目'`（创作板块回填，本板块只读展示） |
| `idempotency_key` | varchar(120) | nullable | 创建/重复提交幂等键；同一工作区内唯一 |
| `deleted_at` | timestamptz | nullable | 灵感软删除时间；默认查询、详情、搜索和计数均排除非空记录 |
| `created_at` | timestamptz | NOT NULL default now() | |
| `updated_at` | timestamptz | NOT NULL default now() | 触发器维护 |

索引：
- `idx_insp_ws_recorded (workspace_id, recorded_at DESC)` —— 按日期/最近列表核心索引
- `idx_insp_ws_type (workspace_id, type_id)` —— 类型筛选
- `idx_insp_search (workspace_id, text_raw)` —— 用 `pg_trgm` GIN 索引支撑关键词检索（见 §5.4）

### 4.4 `assets`（附件 / 媒体文件元数据）

对齐 `06 §3.2`，状态机裁剪到本板块（无 OCR/ASR 态）。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | uuid | PK | |
| `workspace_id` | uuid | FK NOT NULL | |
| `inspiration_id` | uuid | FK→inspirations(id) ON DELETE CASCADE, nullable | 允许「先上传后挂接」 |
| `kind` | varchar(12) | NOT NULL CHECK(kind in ('image','audio','file')) | 对齐前端 `image/audio`，`link` 不入库（外链仅前端记忆） |
| `storage_key` | varchar(255) | NOT NULL | OSS 路径 `{ws}/{uuid}.ext`，DB 与 OSS 唯一契约 |
| `sha256` | char(64) | nullable | complete 时校验 + 去重 |
| `mime_type` | varchar(100) | NOT NULL | 白名单校验（见 §9） |
| `bytes` | bigint | NOT NULL | |
| `duration_ms` | int | nullable | 音频时长（前端展示 `01:24`） |
| `width` / `height` | int | nullable | 图片尺寸 |
| `privacy_scope` | varchar(20) | default 'private' | |
| `status` | varchar(16) | NOT NULL default 'pending' | 见 §8.2：`pending`（已 presign 未传）/ `ready`（已传已校验）/ `failed` |
| `created_at` | timestamptz | NOT NULL default now() | |

索引：
- `idx_assets_insp (inspiration_id)` —— 查某条灵感的全部附件
- `idx_assets_ws_status (workspace_id, status)` —— 清理孤儿（pending 超时）

> 注：前端 `attachments` 中 `kind:'processing'`（OCR 中…）属于 AI 能力，**本板块不生成**；后端不返回该态，前端可保留占位但无数据驱动。

### 4.5 `asset_derivatives`（派生结果，**先建表留空**）

完整保留 `06 §3.2` 结构：`id, asset_id, kind(thumbnail/ocr/transcript/caption/waveform), content_json, model_ref, status, created_at`。本板块不写入；OCR/ASR 上线后由 Worker 填充。

### 4.6 `daily_boards`（白板布局，对齐 `03 §7.1`）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | uuid | PK | |
| `workspace_id` | uuid | FK NOT NULL | |
| `board_date` | date | NOT NULL | 白板所属日期（来自日期滚轮） |
| `layout_version` | int | NOT NULL default 1 | 整理白板后 +1 |
| `created_at` | timestamptz | NOT NULL default now() | |

约束：`UNIQUE(workspace_id, board_date)`。
索引：`idx_boards_ws_date (workspace_id, board_date)`。

> 布局坐标存于 `inspirations.board_position_json`（每张贴片自己的位置），`daily_boards` 只记「该日布局版本 + 整理标记」。前端「整理白板」后批量 PATCH 各灵感坐标并 bump `layout_version`。

---

## 5. 各模块实现方式

### 5.1 灵感内容 CRUD 与二次展示模块

- **创建**：`POST /api/inspirations` 校验正文、类型、发生时间、附件归属和幂等键，写入稳定 `id` 后返回完整对象。
- **二次展示**：首页最近灵感、日期白板、灵感库和搜索结果使用同一后端数据源，摘要项至少包含 `id`、时间、类型名称与颜色、正文摘要、附件概况和 `updated_at`。
- **详情**：`GET /api/inspirations/:id` 返回标题、完整正文、类型、发生时间、附件、同步状态、创建时间和更新时间；软删除记录按不存在处理。
- **编辑**：`PATCH /api/inspirations/:id` 只更新传入字段，修改原记录而非创建副本；类型变更必须校验活动类型，或允许原记录继续保留其已归档类型。
- **删除**：`DELETE /api/inspirations/:id` 先将 `inspirations.deleted_at` 置为当前时间，并将附件置为 `deleting`、写入删除任务；接口返回 `202 Accepted`。OSS 删除通过幂等补偿最终收敛，不宣称跨 RDS/OSS 原子事务。
- 删除受理后，记录立即从默认列表、首页最近灵感、日期白板、搜索结果、当日计数和详情查询中排除。

### 5.2 灵感类型 CRUD 模块

- `GET /api/inspiration-types` 返回系统初始类型与当前工作区活动自定义类型，供快速记录面板左下角 `TypeSelect` 展开列表使用；当前产品不设置独立类型管理页或管理抽屉。
- `GET /api/inspiration-types/:id` 返回单个类型详情和派生字段 `is_system`。
- `POST /api/inspiration-types` 只接收 `label` 与可选 `icon`。若不存在同名记录，则后端生成稳定 `slug`，从 §4.2 六色池随机选择 `color_token` 并持久化；若命中当前工作区已归档同名记录，则将原记录 `archived_at` 恢复为 `NULL`，保留原 `id`、`slug` 和 `color_token`，不新建类型。
- `PATCH /api/inspiration-types/:id` 仅允许修改工作区自定义类型的 `label`、`icon`、`sort_order`；不允许修改系统类型、`slug` 或 `color_token`。
- `DELETE /api/inspiration-types/:id` 由左下角活动自定义类型名称右侧的小型叉号触发，对自定义类型执行 `archived_at=now()`；不删除历史灵感，也不删除类型行。
- 活动名称冲突、系统类型只读、编辑已归档类型和类型不存在分别返回稳定业务错误码；重新添加已归档同名类型属于成功的“重新启用”操作，不返回 `TYPE_ARCHIVED`。

### 5.3 文件上传与关联模块（OSS 预签名直传）

- `POST /api/assets/presign`：API 建立 `assets(status=pending)`，返回 `asset_id` 与短期 PUT 签名 URL。
- 前端使用签名 URL 直传 OSS；浏览器不持有永久 AccessKey。
- `POST /api/assets/complete`：服务端核验对象存在、workspace/key 归属、MIME、实际大小和时长后才置 `ready`。
- 图片或拍照文件最大 20MB；录音或音频最长 60,000ms。前端选择时预检，后端 presign 与 complete 阶段再次校验。
- `pending` 超时对象和失败删除通过补偿任务、生命周期与定期对账清理。

### 5.4 列表与检索模块

- 按日期：`GET /api/inspirations?date=YYYY-MM-DD`。
- 最近灵感：`GET /api/inspirations?recent=N`；当日计数与列表使用相同 `deleted_at IS NULL` 条件。
- 关键词检索：`GET /api/inspirations/search?q=&type_id=&from=&to=`，使用 `pg_trgm` / GIN 支撑标题和正文检索。
- 类型筛选只列活动类型，但历史结果仍可携带已归档类型的快照信息。
- 星云视图仅使用确定性分组或人工关系，不生成或暗示 AI 聚类结果。

### 5.5 白板布局模块

- 读取时随日期列表返回 `board_position_json`。
- 写入时使用稳定灵感 ID，通过 `PATCH /api/inspirations/:id` 或 `POST /api/daily-boards` 批量提交，并维护 `layout_version` 防止覆盖。

### 5.6 草稿与离线兜底（对齐 `03 §5.1`）

本地草稿保存和远端提交是两个不同语义，页面必须提供 **“保存草稿”** 与 **“提交灵感”** 两个独立按钮。

- 用户停止输入约 2 秒后，对标题、正文、类型、发生时间、附件元数据、状态及附件 `Blob` 执行 IndexedDB 本地快照。
- 点击“保存草稿”时立即完整落盘；只有 IndexedDB 事务以及所有图片/音频 `Blob` 写入成功后，才显示“草稿已保存”。任一步失败均显示“草稿保存失败”。
- 点击“提交灵感”前必须先完成一次成功的本地快照；随后执行 RDS 写入、OSS 上传和 `complete` 核验。
- 只有收到表示 RDS 记录与全部附件完整成功的 API 响应后，才显示“已提交/已同步”，并删除对应 IndexedDB 草稿及附件 `Blob`。
- 网络、API、OSS 上传或 `complete` 失败时保留完整本地草稿和编辑内容，显示失败并允许重试，不得误报已同步。

## 6. 内外层调用逻辑与流程

### 6.1 预签名直传 + 灵感提交（主流程）

页面保存语义必须拆成两个按钮，不能再用一个「保存灵感」同时代表本地保存和远端提交：

```
[前端 CapturePage]
  A. 用户停止输入约 2 秒，或点击「保存草稿」
       → 立即生成完整本地快照
       → IndexedDB 写入标题、正文、元数据、附件元数据及图片/音频 Blob
       ├─ 事务成功提交 → 显示「草稿已保存」
       └─ 任一字段或 Blob 写入失败 → 显示「草稿保存失败」
       （此步骤不改变远端 sync_status，不代表已同步）

  B. 用户点击「提交灵感」
       → 先确认本地快照已成功落盘；失败则停止并保留草稿
       → POST /api/inspirations 创建/更新 RDS 灵感记录（提交中）
       → 若有附件：POST /api/assets/presign {kind,mime,bytes,duration_ms}
             API → INSERT/UPDATE assets(pending) → 返回 {asset_id, putUrl}
             前端 fetch(putUrl, PUT, blob) → OSS
             前端 POST /api/assets/complete {asset_id, sha256, ...}
             API → HEAD/校验 OSS 对象 → UPDATE assets SET status=ready
       → API 汇总 RDS 写入、全部附件上传和 complete 结果
       ├─ 收到完整成功响应 → 显示「已提交/已同步」
       │                    → 删除对应 IndexedDB 草稿及附件 Blob
       └─ 任一步骤失败/响应非成功 → 显示提交失败
                            → 保留 IndexedDB 草稿，允许重试
```

**删除本地副本的唯一条件**：已完成远端入库流程，并收到完整成功 API 响应；不能依据按钮点击、请求已发出、单个附件上传成功或页面显示“已保存”来删除 IndexedDB 内容。

### 6.2 离线草稿分支（对齐「不能误报已同步」）

```
完全离线 / 网络中断：
  自动保存或点击「保存草稿」
    → IndexedDB 完整快照成功 → 显示「草稿已保存」
    → 失败 → 显示「草稿保存失败」并保留当前可编辑内容

点击「提交灵感」但无法联网：
  → 不删除 IndexedDB 草稿，不显示「已同步」
  → 显示提交暂不可用/待重试
  → 网络恢复后重新执行 POST /api/inspirations + presign/OSS PUT/complete

联网提交成功后：
  → RDS 记录与 OSS 附件均完成校验
  → 收到完整成功 API 响应
  → 才删除 IndexedDB 对应草稿
```

### 6.3 按日期加载白板（对齐「停止滚动后才预取」）

```
[前端 DateWheel] 用户停止滚动（防抖 ~180ms）
  → GET /api/inspirations?date=YYYY-MM-DD
      API → SELECT ... WHERE workspace_id AND date(recorded_at)=:date
            ORDER BY recorded_at
      → 返回 [{id,type,text,recorded_at,assets:[...],board_position_json}, ...]
[前端] 渲染白板贴片 / 星云节点
```

### 6.4 灵感软删除与附件补偿

```text
DELETE /api/inspirations/:id
  API → BEGIN
        锁定并校验当前 workspace 下未删除的灵感
        UPDATE inspirations SET deleted_at=now()
        UPDATE assets SET status='deleting', deleted_at=COALESCE(deleted_at, now())
        INSERT asset_deletion_tasks / outbox（每个 storage_key 幂等）
        COMMIT
  → 返回 202 { data:{ id, status:'deleting' } }
  补偿任务 → 幂等删除 OSS 对象 → 标记任务完成
  定期对账 → 重试失败任务，发现孤儿对象与悬空元数据
```

数据库事务只保证 RDS 内状态与删除任务一致，不保证 OSS 已在响应前删除。前端收到 `202` 后应立即从各视图移除灵感，但不得把该响应解释为对象存储已完成物理清理。

## 7. 后端接口设计（REST）

所有接口沿用项目响应包装 `{ data, meta? }`，所有查询与写操作均校验当前 `workspace_id`。错误体统一包含可读消息与稳定 `code`。

### 7.1 灵感创建

- `POST /api/inspirations`
- 请求：`{ id?, draft_id?, idempotency_key?, title?, text_raw, type_id?, recorded_at?, user_tags_json?, board_position_json?, is_quote?, sync_status?, asset_ids? }`
- 响应 `201`：`{ data: InspirationDetail }`。
- 校验：正文/标题至少一项有效；`type_id` 必须是系统初始类型或当前工作区活动自定义类型；重复幂等键返回同一记录而不是重复创建。

### 7.2 灵感列表与搜索

- `GET /api/inspirations?date=YYYY-MM-DD&recent=N&type_id=&page=&page_size=`
- 响应 `200`：`{ data:[InspirationSummary], items:[...], meta:{ page,page_size,total,has_more,today_count,date_count,layout_version } }`。
- `GET /api/inspirations/search?q=关键词&type_id=&from=&to=&page=&page_size=`
- 响应 `200`：摘要列表与分页信息。
- 所有默认查询、计数和搜索必须带 `deleted_at IS NULL`。

### 7.3 灵感详情

- `GET /api/inspirations/:id`
- 响应 `200`：`{ data: InspirationDetail }`，包含 `assets`、类型对象、`created_at` 和 `updated_at`。
- 不存在、越权或已软删除统一按资源不可见处理，返回 `404` 或项目既定不泄露策略。

### 7.4 灵感更新

- `PATCH /api/inspirations/:id`
- 请求：创建字段的可更新子集；`id`、`workspace_id`、`idempotency_key`、`created_at`、`deleted_at` 不允许客户端修改。
- 响应 `200`：`{ data: InspirationDetail }`，返回更新后的原记录。
- 若当前灵感仍引用已归档类型，可不修改该类型；一旦切换类型，只允许选择活动类型。

### 7.5 灵感删除

- `DELETE /api/inspirations/:id`
- 行为：软删除灵感、附件进入 `deleting`、写入删除补偿任务。
- 响应 `202 Accepted`：

```json
{
  "data": {
    "id": "uuid",
    "status": "deleting"
  }
}
```

- 不使用 `204 + RDS/OSS 原子级联删除` 口径。

### 7.6 附件预签名与收尾

- `POST /api/assets/presign`
  - 请求：`{ kind:'image'|'audio'|'file', mime_type, bytes, duration_ms?, inspiration_id? }`
  - 响应 `201`：`{ data:{ asset_id, put_url, storage_key, expires_in } }`
  - 图片 `bytes ≤ 20MB`；录音/音频 `duration_ms ≤ 60,000`。
- `POST /api/assets/complete`
  - 请求：`{ asset_id, sha256?, bytes?, width?, height?, duration_ms? }`
  - 响应 `200`：`{ data:{ asset_id, status:'ready' } }`
  - 服务端再次核验对象存在、归属、大小、Content-Type 和音频时长；超限或不一致不得置为 `ready`。

### 7.7 灵感类型 CRUD

#### 7.7.1 类型列表

- `GET /api/inspiration-types?include_archived=false`
- 默认返回系统初始类型和当前工作区活动自定义类型，直接渲染在快速记录面板左下角展开列表；普通界面不展示归档列表，也不设置独立管理页。`include_archived=true` 仅保留给受控的维护、审计或测试场景。
- 响应项统一为：

```json
{
  "id": "uuid",
  "workspace_id": null,
  "slug": "idea",
  "label": "想法",
  "color_token": "--nebula-violet",
  "icon": null,
  "sort_order": 10,
  "archived_at": null,
  "is_system": true
}
```

#### 7.7.2 类型详情

- `GET /api/inspiration-types/:id`
- 响应 `200`：`{ data: InspirationType }`；不可见或不存在返回 `404 TYPE_NOT_FOUND`。

#### 7.7.3 新增或重新启用自定义类型

- `POST /api/inspiration-types`
- 请求：`{ "label":"观察", "icon":null }`；前端不提交 `workspace_id`、`slug` 或 `color_token`。
- 后端先按项目既有规则裁剪、规范化名称，并在当前工作区内检查活动及已归档同名记录：
  1. **无同名记录**：生成稳定 `slug`，从六色白名单随机选择并持久化 `color_token`；响应 `201 Created`，例如 `{ data: InspirationType, meta:{ operation:'created' } }`。
  2. **存在已归档同名记录**：执行 `UPDATE inspiration_types SET archived_at = NULL WHERE id = :id RETURNING ...`；响应 `200 OK`，例如 `{ data: InspirationType, meta:{ operation:'reactivated' } }`。必须保留原 `id`、`slug`、`color_token`，不重新随机颜色、不创建新行。
  3. **存在活动同名记录**：返回 `409 TYPE_LABEL_EXISTS`，不得改变现有类型。
- 前端根据响应中的类型对象渲染圆圈；当 `meta.operation='reactivated'` 时提示“已重新启用该类型”。
- 名称检查与创建/恢复必须在事务和唯一约束保护下完成，避免并发请求产生两个活动同名类型。

#### 7.7.4 编辑自定义类型

- `PATCH /api/inspiration-types/:id`
- 请求：`{ label?, icon?, sort_order? }`。
- 响应 `200`：更新后的类型；`color_token` 与 `slug` 保持不变。
- 系统初始类型返回 `403 TYPE_READ_ONLY`；已归档类型返回 `409 TYPE_ARCHIVED`。

#### 7.7.5 删除/归档自定义类型

- `DELETE /api/inspiration-types/:id`
- 行为：由左下角类型列表中自定义类型名称右侧的小型叉号触发；确认后设置 `archived_at=now()`，不删除历史灵感。
- 响应 `200`：`{ data:{ id, archived_at, status:'archived' } }`。
- 系统初始类型返回 `403 TYPE_READ_ONLY`；不存在返回 `404 TYPE_NOT_FOUND`。

### 7.8 白板布局

- `POST /api/daily-boards`（或批量 `PATCH`）
- 请求：`{ board_date, positions:[{ inspiration_id,x,y,z,rotation }], layout_version }`
- 响应 `200`：新 `layout_version`；版本冲突返回明确错误，防止覆盖其他更新。

## 8. 与前端对接约定

### 8.1 API 字段映射

| 页面字段 / 用途 | API 字段 | 说明 |
|---|---|---|
| 灵感稳定标识 | `id` | 卡片 key、详情、编辑和删除均使用该 UUID，不使用数组索引 |
| 标题 / 正文 | `title` / `text_raw` | 摘要由前端截断，详情展示完整内容 |
| 类型 | `type_id` + `type` | `type` 至少包含 `id,label,color_token,archived_at,is_system` |
| 时间 | `recorded_at` | API 使用带时区 ISO 字符串；页面按业务时区格式化 |
| 白板位置 | `board_position_json` | 绑定稳定灵感 ID |
| 附件 | `assets` | 每个附件包含独立 `status`，未 `ready` 不展示为成功 |
| 用户标签 | `user_tags_json` | 与灵感类型分离 |
| AI 标签 | `ai_tags_json` | 本期为空，不展示为自动融合结果 |
| 同步状态 | `sync_status` / `processing_status` | 区分本地草稿、提交中、已同步和失败 |
| 审计时间 | `created_at` / `updated_at` | 详情展示，更新后用于缓存失效 |
| 删除状态 | `deleted_at` | 默认响应不返回已删除记录；管理/审计接口另行授权 |

### 8.2 状态机

- 本地草稿：`local_editing → local_saving → local_saved / local_save_failed`。
- 远端提交：`submitting → synced / submit_failed`；完整成功前不清除 IndexedDB。
- 灵感级：`sync_status='local'|'synced'`，`processing_status='draft'|'synced'|'failed'`。
- 附件级：`pending → ready / failed → deleting → deleted`；每个附件独立显示状态。
- 类型级：活动自定义类型 `archived_at=null`；点击叉号删除后为已归档，只影响后续选择，不破坏历史展示；同名重新添加时原记录从已归档恢复为活动。

### 8.3 类型颜色、圆圈与重新启用渲染

- 系统初始类型按 §4.2 固定颜色展示，且选项右侧不显示删除叉号。
- 自定义类型创建表单只提交 `label` 与可选 `icon`；当前 `CapturePage.jsx` 固定发送 `--ink-muted` 的行为属于待修复缺口。
- 提交按钮进入加载状态。真正新建收到 `201/meta.operation='created'` 后使用响应中的 `color_token` 添加选项；重新启用收到 `200/meta.operation='reactivated'` 后使用原记录的 `color_token` 恢复选项。失败时不插入伪类型。
- 前端只能使用六色白名单或系统种子颜色映射 CSS 变量，不能客户端二次随机，也不能在刷新、重命名或重新启用时改色。
- 活动自定义类型名称右侧显示小型叉号；叉号点击区与类型选中区分离，点击叉号不得同时触发选中。归档成功后从活动列表移除；如它是当前未提交记录的选中类型，则回退为“先不分类”并提示用户。
- 已归档类型在历史卡片和详情中继续使用原颜色，并附加“类型已停用”文案；普通选择器不再显示。重新添加同名类型成功后，原类型重新出现在列表中，并提示“已重新启用该类型”。

### 8.4 UI 组件与接口矩阵

| UI 组件 | 主要职责 | 接口 |
|---|---|---|
| `TypeSelect` | 位于快速记录面板左下角；选择活动系统/自定义类型并显示颜色圆圈；承载当前类型管理入口 | `GET /api/inspiration-types` |
| `TypeCreateForm` | 位于列表底部；输入名称和可选图标；根据响应区分真正新建与同名已归档类型重新启用 | `POST /api/inspiration-types` |
| `TypeOptionRow` | 展示单个类型；仅活动自定义类型名称右侧显示小型叉号，并将选中与停用点击区分离 | `GET /api/inspiration-types`、`GET/PATCH/DELETE /api/inspiration-types/:id` |
| `TypeArchiveConfirmPopover` | 在当前列表上下文轻量说明“停用类型不会删除历史灵感”，确认归档；不形成独立管理页/抽屉 | `DELETE /api/inspiration-types/:id` |
| `InspirationSticky` | 首页/白板/灵感库/搜索结果摘要二次展示 | 列表或搜索接口 |
| `InspirationDetailDrawer` | 展示完整正文、附件、类型和审计时间 | `GET /api/inspirations/:id` |
| `InspirationEditForm` | 编辑原记录，失败时保留输入 | `PATCH /api/inspirations/:id` |
| `InspirationDeleteConfirmDialog` | 二次确认并在 `202` 后移除各视图数据 | `DELETE /api/inspirations/:id` |

更新成功后，应按稳定 `id` 更新或失效首页、白板、灵感库、搜索结果和当日计数缓存；删除受理后同步移除。任一接口失败时不得提前显示成功，编辑输入与本地草稿应继续保留。

## 9. 安全与约束（本板块必要项，对齐 `06 §7` 裁剪）

- **workspace 隔离**：所有查询带 `workspace_id` 条件（中间件注入），防越权。
- **OSS 短期签名 URL**：`presign` 返回 300s 内有效的 PUT URL；密钥仅服务端持有，不下发前端。
- **MIME / 文件限制**：图片/拍照的 MIME 必须为 `image/jpeg,png,webp,gif`，单个文件不得超过 **20MB**；录音/音频的 MIME 必须为 `audio/webm,mp4,mp3,m4a`，业务时长不得超过 **60,000ms（1 分钟）**。如保留音频传输字节上限，则 `≤100MB` 仅表示传输大小上限，与 60 秒时长限制同时生效。
- **限制校验时机**：前端选择/拍摄/录音完成后立即预检查；`presign` 阶段服务端校验声明的 MIME、字节数和 `duration_ms`；`complete` 阶段服务端基于 OSS 对象再次核验实际大小、类型和音频时长。任何超限文件均返回明确失败原因，不得获得 `ready` 状态，也不得从 IndexedDB 草稿中删除。
- **图片 EXIF**：RDS 与 OSS 不会自动清理 EXIF。只有在前端重编码、服务端代理处理或已验证的 OSS 图片处理链路真正启用时，才能承诺清理；否则必须把原始 EXIF 风险作为已知受限点披露。
- **删除一致性**：不能把数据库事务和 OSS 删除宣称为单一原子操作；采用软删除、待处理记录、幂等重试和定期对账，最终收敛而非强一致。
- **CORS**：OSS 桶配置允许前端域名的 PUT 跨域；若不便配置，改「服务端代理上传」（前端 PUT 到 API，API 转 OSS），代价是走应用带宽，但本板块文件量小可接受。
- **密钥管理**：OSS AccessKey / RDS 密码走环境变量或密钥管理服务，禁止硬编码。

---

## 10. 部署与运行要点（RDS Serverless + OSS）

- **RDS PostgreSQL Serverless**：使用连接池（`pg.Pool`），因 Serverless 实例可能有冷启动/连接回收，建议 `max: 5~10`、`idleTimeout` 配短，并启用 `application_name`；首次冷连接延迟可接受（本板块非高频写）。
- **OSS**：华东/与 RDS 同地域，降低延迟；开启「防止同名覆盖」由 `uuid` 文件名天然保证；生命周期规则清理 `pending` 超时孤儿对象。
- **本地开发**：前端 `vite` 代理 `/api` → `http://127.0.0.1:3210`（沿用 `Code-8` 监听习惯）；后端 `npm run dev` 起 Express。
- **前端改造点（替换 mock）**：`src/data/inspirations.js` 改为调用上述接口；`TimelinePage` 在日期滚轮停止滚动后 `GET ?date=`；`CapturePage` 拆分为「保存草稿」（IndexedDB 本地快照）和「提交灵感」（RDS + OSS 完整流程，成功 API 响应后才清理 IndexedDB）；`HomePage`「最近灵感」改 `GET ?recent=`。

---

## 11. 验收对照（对齐 `03 §13` 与 `07`）

- 新用户可在 30 秒内记录一条带图灵感，创建、预签名、上传和 complete 链路可验证。
- 自动保存和“保存草稿”只有在 IndexedDB 字段、元数据和全部 `Blob` 事务成功后才显示成功；失败明确提示且可继续编辑。
- “提交灵感”只有在 RDS 记录与全部 OSS 附件完整成功并收到 API 回执后，才清理 IndexedDB 草稿。
- 图片超过 20MB、录音/音频超过 60 秒时，前端预检与后端校验均拒绝，附件不得进入 `ready`。
- 类型列表、详情、新增/重新启用、编辑、删除/归档五个接口均通过正常、越权、重复名称、系统类型只读与已归档用例。
- 真正新建自定义类型的 `color_token` 由后端产生，100% 命中六色白名单且不为 `--ink-muted`；数据库保存值与后续查询一致。
- 类型重命名、刷新和服务重启后颜色保持不变；归档类型不删除历史灵感，历史详情可显示“类型已停用”。同名重新添加后原记录 `archived_at` 恢复为 `null`，数据库类型记录数不增加，原 `id`、`slug`、`color_token` 均保持不变。
- 灵感创建后可从首页、日期白板、灵感库和搜索结果二次展示，并能打开同一完整详情。
- 灵感编辑修改原记录、更新 `updated_at`，不会额外创建重复记录；保存成功后各可见视图同步更新。
- 灵感删除返回 `202`，并立即从默认列表、详情、白板、搜索和计数中排除；OSS 删除失败可通过补偿最终收敛。
- 类型管理始终位于快速记录面板左下角展开列表：系统类型无叉号，自定义类型名称右侧有小型叉号；不新增独立管理页面或管理抽屉。接口失败时 UI 不提前显示保存、编辑、归档、重新启用或删除成功；用户输入和可恢复草稿不丢失。
- 本期不展示或承诺 OCR、ASR、AI 标签自动融合结果。

## 12. 补充：PWA 拍照与录像（纯客户端能力）实现步骤

> 本节补全「PWA 拍照与录像相关 API 接入」这一**纯前端 / 客户端功能**的实现步骤。它与 §0 评估结论一致：**不引入 OCR/ASR/AI，也不引入任务队列 / Worker**，仅作为客户端采集能力落地，并通过唯一契约点（§12.2 `assetUpload.js`）与后端 presign/complete 解耦对接。

### 12.1 定位澄清：PWA "拍照 / 录音 API" 是什么

- 它**不是服务端 API**，而是浏览器客户端能力：`navigator.mediaDevices.getUserMedia`（取摄像头/麦克风流）+ `MediaRecorder`（录音）+ `canvas.toBlob`（拍照截图）。全程在客户端完成、产出 `Blob`，**不依赖任何后端即可开发与本地验证**。
- 与后端的关系**只有一个契约点**：`src/lib/assetUpload.js` —— 拿到 `Blob` 后，若配置了 `VITE_API_BASE` 就走 §6.1 的 presign → OSS 直传 → complete；若未配置（纯前端 / 离线）就落 IndexedDB 草稿兜底（§6.2）。其余流程与后端完全解耦。
- 因此本能力属于"纯前端 / 客户端功能"，与本文档「不含 OCR/ASR/AI 融合」的边界一致，也不引入任务队列 / Worker。

### 12.2 已实现代码清单（`Nebula-React`，已落地并通过构建）

| 文件 | 职责 |
|---|---|
| `public/manifest.webmanifest` | PWA 清单：`name` / `display:standalone` / `orientation:portrait` / `theme_color` / `icons`（品牌 SVG） |
| `public/sw.js` | Service Worker：**network-first** 缓存 app shell，失败回退缓存；**不缓存 `/api/`**（避免读过期数据） |
| `src/lib/secure.js` | `isSecureContextSupported()` —— `getUserMedia`/`MediaRecorder` 仅在 HTTPS / localhost 可用 |
| `src/lib/draftStore.js` | IndexedDB 草稿库：`putDraft` / `getDraft` / `getAllDrafts` / `deleteDraft`，离线 / 无后端时暂存 `Blob` |
| `src/lib/assetUpload.js` | `uploadCapturedAsset()` —— **唯一契约点**：后端配置走 presign/complete，否则 IndexedDB 草稿兜底 |
| `src/components/MediaCaptureOverlay.jsx` + `.css` | 拍照 / 录音浮层：`getUserMedia` 取流 → `canvas` 截图（拍照）/ `MediaRecorder`（录音）；错误本地化 |
| `src/components/ModeToolbar.jsx` | 拍照 / 录音按钮**仅在安全上下文启用**，否则 `disabled` + 提示（不再是无条件占位） |
| `src/pages/CapturePage.jsx` | 接入浮层、`handleCaptured`（预览 + 上传 + 状态）、移除附件回收 `blob:` URL、失败 `note` 提示 |
| `src/main.jsx` | 生产环境注册 Service Worker（`import.meta.env.PROD`） |
| `index.html` | `<link rel="manifest" href="/manifest.webmanifest">` |

### 12.3 关键实现要点

- **安全上下文门控**：`secure.js` 判 `window.isSecureContext`；`ModeToolbar` 据此 `disabled`；`CapturePage` 在非安全上下文点击时显示 `note`（"需 HTTPS 或 localhost"），不崩溃。
- **设备错误本地化**：`MediaCaptureOverlay.describeError()` 将 `NotAllowedError` / `NotFoundError` / `NotReadableError` / `OverconstrainedError` 映射为中文提示，**不向上抛异常打断编辑**。
- **资源回收**：`removeAttachment` 时若 `src` 以 `blob:` 开头则 `URL.revokeObjectURL`，防内存泄漏。
- **附件独立状态（对齐 §8.2）**：`handleCaptured` 先置 `pendingUpload:true`；上传成功置 `false + synced`；失败置 `failed:true` + `note`。**绝不**以全局进度掩盖单个失败。
- **媒体类型映射**：拍照 → `image/jpeg` Blob → 前端 `image` 附件；录音 → `MediaRecorder` 产出 `webm` Blob → 前端 `audio` 附件（复用既有音频波形贴片）。`kind` 严格落在 `assets.kind` 枚举内。
- **SW 缓存策略**：仅缓存同源 GET 静态资源与导航；`/api/` 不缓存 → 数据始终取最新，弱网 / 离线仅保证 app shell 可开。

### 12.4 接入步骤（开发 → 生产）

1. **本地开发（localhost）**：拍照 / 录音直接可用（localhost 属安全上下文）；无后端时 `Blob` 落 IndexedDB 草稿，刷新仍在。
2. **接后端（配置 `VITE_API_BASE`）**：`uploadCapturedAsset` 自动切到 presign/complete，`Blob` 直传 OSS，落 `assets` 表（§6.1）。**无需改 `CapturePage` 其他逻辑**。
3. **生产 PWA**：部署到 HTTPS 域名；`manifest.webmanifest` + `sw.js` 随 `dist/` 产出；浏览器可"添加到主屏幕"，拍照 / 录音在真机可用。
4. **权限与设备**：须 HTTPS；iOS Safari 对 `getUserMedia` 有限制（需用户手势触发、部分版本仅 Safari 支持），Android Chrome 完整支持。

### 12.5 本次验证结果

- `npm run build`：64 modules transformed，0 报错（CSS 35.73 KB / JS 190.62 KB gzip 63.56 KB）。
- `vite preview` 冒烟：`/`、`/manifest.webmanifest`、`/sw.js`、`/assets/svg/capture/camera.svg`、`/assets/svg/capture/record.svg`、`/assets/svg/common/brand-mark.svg` **全部 200**。

---

## 13. 从搭建到可上线版本服务的完整步骤清单

> 覆盖：环境配置、构建与部署、PWA 功能接入（含拍照与录像）、服务可用性保障、可靠性设计、异常处理。
> 约束：**最终服务可正常运行、稳定可靠且无缺陷，且不得引入任何额外功能**（OCR/ASR/AI 标签融合、任务队列、Worker、AI 网关均不在本清单内）。

### 13.0 前提与边界

- 范围：灵感记录板块（`/capture` 记录、`/timeline` 灵感库、`/` 首页最近），不含 OCR/ASR/AI 标签融合。
- 所需能力：RDS PostgreSQL Serverless（元数据）+ OSS（字节）+ 应用 API（Express，代码层）+ 前端 PWA（客户端拍照/录音）。
- 不引入：任务队列 / Worker / AI 网关 / `asset_derivatives` 写入（表先建留空）。

### 13.1 环境配置

1. 运行环境：Node 18+ / npm；本地或 CI 安装依赖。
2. RDS PostgreSQL Serverless 实例：建库、配置 IP 白名单、记录连接串。
3. OSS 桶：与 RDS 同地域；配置 CORS 允许前端域名的 `PUT` 跨域（或改"服务端代理上传"，见 §9）。
4. 后端环境变量：`DATABASE_URL`、`OSS_REGION`/`OSS_BUCKET`/`OSS_ACCESS_KEY`/`OSS_SECRET`、`API_PORT`（沿用 `3210`）、`CORS_ORIGIN`、`PRESIGN_TTL`（如 300s）。**密钥走环境变量，禁止硬编码**。
5. 前端 `.env`：`VITE_API_BASE` 指向网关 `/api`（空则走本地 IndexedDB 草稿）。

### 13.2 数据层搭建（RDS）

6. 执行 §4 建表 SQL：`workspaces` / `inspiration_types` / `inspirations` / `assets` / `asset_derivatives`（留空）/ `daily_boards`。
7. 建索引（§4.3 `idx_insp_ws_recorded`、§4.4 `idx_assets_insp`、§4.6 `idx_boards_ws_date`）并启用 `pg_trgm` 扩展（§5.4 检索）。
8. 灌入 7 个系统初始类型种子；为类型活动名称约束、软删除字段和灵感 `idempotency_key/deleted_at` 建立迁移与索引。

### 13.3 后端搭建（Express，代码层）

9. 初始化 `server/`（§2.3 结构），安装 `express` / `pg` / `ali-oss` / `zod` / `cors`。
10. `db.js` 连接池（Serverless：`max 5~10`、短 `idleTimeout`、`application_name`）；`oss.js` presign 封装；`auth.js` workspace 中间件；`validators.js` 入参校验。
11. 实现并验证路由（§7）：灵感创建/列表/搜索/详情/编辑/软删除；类型列表/详情/新增/编辑/归档；附件 presign/complete；白板布局。
12. 实现 §6 流程：presign → 直传 → complete；灵感软删除 + 附件删除任务（§6.4）；`pending` 超时孤儿清理和 OSS 删除补偿。
13. 安全（§9 全量）：workspace 隔离、系统类型只读、短期签名 URL、MIME/大小/时长白名单、可验证的 EXIF 处理策略、软删除补偿、密钥环境变量。

### 13.4 前端搭建与改造（`Nebula-React`）

14. 复用现有 React/Vite 工程与 `tokens.css` **零翻译**约定（变量名与设计稿一致，不新建映射层）。
15. 替换 mock（§10）：`CapturePage` 接入创建与附件链路；在现有左下角类型列表内补齐 `TypeSelect/TypeOptionRow/TypeCreateForm/TypeArchiveConfirmPopover`，不新增独立管理页面或抽屉；首页、白板、灵感库和搜索结果接入统一摘要与详情组件。
16. 补齐 `InspirationDetailDrawer/InspirationEditForm/InspirationDeleteConfirmDialog`，编辑或删除成功后按稳定 `id` 更新全部可见视图；失败时保留输入。PWA 继续复用现有草稿与采集能力。

### 13.5 构建与部署

17. 前端：`npm install && npm run build` → `dist/`（自动含 `manifest.webmanifest`、`sw.js`）。静态托管（CDN / OSS 静态网站 / 对象存储静态站）。
18. 后端：构建或容器化，监听 `127.0.0.1:3210`（或置于网关后）；生产由 **HTTPS 网关统一入口**，`/api` 反向代理到后端。
19. HTTPS：全站 HTTPS（PWA 拍照/录音硬要求）；OSS 用 HTTPS 域名；CORS 配 HTTPS 前端域。
20. 数据库迁移纳入发布流程（`migrations/` 版本化，可重复执行幂等）。

### 13.6 PWA 功能接入（含拍照与录像）

21. 已落地（§12）：manifest 清单、SW 注册、安全上下文门控、`getUserMedia`/`MediaRecorder`、IndexedDB 兜底、附件独立状态。
22. 生产核对：HTTPS 可达；`manifest` 合法（Lighthouse PWA 自检通过）；SW 在 `PROD` 注册；真机（Android Chrome / iOS Safari）拍照 / 录音权限正常。
23. 回退：非安全上下文（http 且非 localhost）按钮禁用并提示，**编辑 / 保存等其余功能不受影响**。

### 13.7 服务可用性保障

24. 健康检查：`GET /api/health`（RDS ping + OSS 可达）→ 供探针 / 网关存活判断。
25. 连接池与冷启动：RDS Serverless 连接回收策略（§10）；应用无状态、可水平扩容。
26. 静态资源 CDN 缓存 + SW app shell 缓存 → 弱网 / 离线可打开；`/api/` 不缓存保证数据新鲜。
27. 基础限流：API 按 workspace / IP 限流，防突发（本板块文件量小，轻量即可）。

### 13.8 可靠性设计

28. 预签名直传：字节不经应用服务器，OSS 直传，降低 API 负载与超时风险。
29. 事务与一致性：RDS 内软删除灵感、附件状态和删除任务同事务提交；OSS 通过幂等补偿最终删除（§6.4），不宣称跨资源原子事务。
30. 幂等与去重：灵感创建使用 `idempotency_key`；`complete`、灵感删除和附件删除任务均可安全重试；客户端 SHA-256 仅作为辅助信息。
31. 状态可观测：灵感、附件与类型归档状态均有稳定字段；前端每附件独立状态，不掩盖部分失败，并区分删除已受理与 OSS 已物理清理。
32. OSS 生命周期：`pending` 超时对象自动清理；文件名用 `uuid` 天然防覆盖。

### 13.9 异常处理

33. 设备 / 权限异常（PWA）：`describeError` 中文映射，编辑不被打断；失败附件标红 + 可重试（§12.3）。
34. 上传中断：`complete` 超时 → `assets` 保持 `pending` → 孤儿清理；前端捕获异常 → `failed` 态 + `note`，`Blob` 仍留 IndexedDB 草稿待补传。
35. 本地保存异常：IndexedDB 事务或图片/音频 `Blob` 写入失败 → 显示「草稿保存失败」，不得显示「已保存」或宣称附件可恢复。
36. 网络异常：离线时 IndexedDB 草稿；恢复后补 `POST` + presign/complete（§6.2 / §12）；完整 API 成功响应前不删除本地草稿。
37. API 错误：统一错误体 `{ error, code }`；补齐 `TYPE_LABEL_EXISTS/TYPE_READ_ONLY/TYPE_ARCHIVED/TYPE_NOT_FOUND` 等稳定业务码；`POST` 命中已归档同名类型时返回成功的 `meta.operation='reactivated'` 而非 `TYPE_ARCHIVED`；前端不静默失败。
38. SW 失败：`install`/`activate` 容错；`fetch` 失败时回退缓存 app shell 或 `'/'`，**不白屏**。

### 13.10 上线前自检（无缺陷兜底）

39. 构建零报错（`npm run build`）；`preview` 全部路由 200（含 manifest / sw.js / 图标 SVG）。
40. 验收对照 §11 全过，重点覆盖类型完整 CRUD、左下角内联管理与叉号停用、六色随机且非灰色、颜色持久稳定、同名归档类型恢复原记录，以及灵感详情/编辑/删除的多视图同步。
41. 安全自检 §9 全过（密钥无硬编码、CORS 收紧、MIME/大小/时长白名单、系统类型只读、软删除与补偿）。
42. PWA 自检（§13.6.22）全过；真机拍照 / 录音跑通。
43. **不引入额外功能**：代码审查确认无 OCR/ASR/AI/任务队列/Worker 混入；`asset_derivatives` 仅建表不写入。

---

## 14. 阿里云 RDS PostgreSQL Serverless + OSS 专项评判与风险评估

> 评估对象：本文件所描述的「灵感记录 / 灵感库」后端方案及当前 `Nebula-React` 实现
> 评估范围：灵感记录、编辑、按日期回看、搜索筛选、白板布局、附件上传/预览/删除；**不涉及 OCR、ASR、AI 标签自动融合**
> 资料核验日期：2026-08-19；地域、版本、规格、配额和端点策略最终以阿里云目标账号控制台实时可选项为准

### 14.1 总体判断：有条件可行，不是无条件可行

**判定：有条件可行（Conditional Go），综合风险中等。**

RDS PostgreSQL Serverless 适合保存灵感、工作区、类型、附件元数据、状态、时间和白板布局等结构化数据；OSS 适合保存图片、音频等二进制对象。排除 OCR、ASR 和 AI 标签自动融合后，首期可以不引入任务队列、Worker 或 AI 网关，业务链路可收敛为 CRUD + 附件状态机 + 日期/关键词查询。

但是，本文件第 0 节的“仅使用 RDS + OSS 足以支撑完整功能”需要作边界修正：**RDS + OSS 只是数据层，不会自行提供业务 REST API、用户认证、workspace 隔离、预签名生成、上传完成核验、幂等、错误处理、迁移、审计、删除补偿或定时清理。**本方案完整落地至少还需要一个可运行的 Application API/Express 运行时：

- 本地测试：可在本机运行 Node + Express，仅用于开发联调；
- 生产部署：可使用同地域的函数计算、SAE、ECS、容器或其他受控 API 运行环境；
- API 负责所有权限判断、RDS CRUD、OSS 签名、`complete` 校验、状态迁移和可观测性；
- 浏览器永远不能直连 RDS，也不能持有 OSS 永久 AccessKey。

因此：

- **“RDS + OSS + API 运行时”**：对当前灵感记录范围可行；
- **“只有 RDS + OSS，不提供 API 运行时”**：不可行，受限点是没有任何地方承载鉴权、业务逻辑和跨资源一致性。

### 14.2 当前实现差距与完整性风险

当前代码已经具备部分真实后端能力，但仍不能把“接口文件存在”直接等同为本次需求闭环完成：

1. `server/src/routes/types.js` 当前只有 `GET /api/inspiration-types` 与 `POST /api/inspiration-types`，尚未实现 `GET/PATCH/DELETE /api/inspiration-types/:id`，因此自定义类型查看详情、编辑和通过左下角叉号删除/归档尚未形成后端闭环。
2. 当前类型创建校验允许并默认接收 `color_token`，`CapturePage.jsx` 又固定发送 `--ink-muted`；这与“真正新建时由后端从六个初始非灰色颜色中随机选择并持久化”的规则直接冲突。当前 `POST` 也未形成“命中已归档同名类型时恢复原记录、保留原 ID/slug/颜色且不新增行”的明确闭环，均必须视为未完成项。
3. `server/src/routes/inspirations.js` 已存在创建、列表、搜索、详情、更新与软删除接口；删除已设置 `deleted_at`、将附件置为 `deleting`、写入删除任务并返回 `202`。这部分是可复用基础，但仍需接口契约测试确认字段、权限、无效类型、幂等和已删除记录排除逻辑。
4. 前端当前没有完整的 `InspirationDetailDrawer`、编辑表单和删除确认闭环，也没有保证编辑/删除后首页、白板、灵感库、搜索结果和计数统一刷新的机制；因此“后端接口存在”尚不足以满足产品可用性。
5. 目标类型管理 UI 是现有快速记录面板左下角展开列表，而不是新页面或 `TypeManagerDrawer`；自定义类型名称右侧叉号、系统类型无叉号、点击区隔离、归档后的历史展示与普通选择器排除、同名归档类型重新启用均需前后端共同实现和验证。
6. 草稿、附件和远端提交仍必须以真实 IndexedDB/RDS/OSS 回执驱动状态；页面提示、定时器或局部状态变化不能作为已落盘、已同步证据。
7. 当前页面中的 OCR、转写、AI 类型建议或 AI 标签演示字段不属于本期范围，必须隐藏、禁用或标注未启用。
8. 白板坐标若承诺可编辑，必须使用稳定灵感 ID 与版本化布局持久化；若暂不持久化，应明确为固定展示。

### 14.3 推荐目标架构与职责边界

```text
Nebula-React 浏览器
  ├─ HTTPS 调用 Application API
  └─ 使用 API 签发的短期 OSS PUT/GET URL
             │
             ▼
Application API / Express（必须存在）
  ├─ 认证、workspace 隔离、输入校验、限流、幂等
  ├─ inspirations / assets / types / daily-boards CRUD
  ├─ 生成固定前缀 object key 与短期签名
  ├─ HEAD Object 核验上传完成
  └─ 删除补偿、孤儿对象对账、日志与指标
       │                         │
       ▼                         ▼
RDS PostgreSQL Serverless       OSS private Bucket
结构化元数据、状态、任务记录     图片/音频对象
```

建议职责：

- RDS 保存 `users/workspaces/inspirations/assets/daily_boards` 等首期必要实体，以及对象 key、大小、MIME、上传状态、软删除状态、版本和时间戳；
- OSS 只保存对象本体，不把永久公网 URL 当作业务授权依据；
- 附件至少区分 `pending/ready/failed/deleting/deleted`；主灵感和附件状态解耦；
- OCR/ASR/AI 衍生结果不属于本期必需数据。`asset_derivatives` 可以预留迁移位，但“建表”不等于能力可用；
- RDS 事务只能覆盖数据库。OSS 删除、对象存在性与数据库状态之间采用最终一致、幂等重试和对账。

### 14.4 本地测试配置要点

#### 14.4.1 地域选择

1. RDS、OSS 和未来 API 运行时优先同地域部署，以便生产 API 使用内网地址和 OSS 内网 Endpoint，降低时延与公网流量成本。
2. 地域不能只按“华东”这类文案预设；应在目标账号控制台确认 RDS PostgreSQL Serverless 可创建、目标 PostgreSQL 版本可用、OSS 端点策略可用，以及未来 API 运行环境可部署。
3. 中国内地使用 OSS 时，必须单独验证目标账号的公共 Endpoint 规则。阿里云 OSS 文档提示：自 2025-03-20 起，中国内地地域的新 OSS 用户使用默认公共 Endpoint 进行数据 API 操作存在限制，可能需要绑定自定义域名；中国内地自定义域名还可能涉及 ICP 备案。
4. 如果本地浏览器直传因端点规则不能工作，备选路径是：绑定合规自定义域名；测试期使用非中国内地 OSS 地域；或让同地域云上 API 代理上传。每条路径都会带来备案、跨境/时延/费用或 API 带宽压力，必须在测试记录中明确取舍。

#### 14.4.2 RDS Serverless 最小测试规格与连接

建议基线：

- PostgreSQL 主版本选择团队已验证且目标地域控制台支持的版本；
- 按官方当前说明，活跃 Serverless 实例最低计算能力一般从 **0.5 RCU** 起；开发测试可将最大 RCU 先限制在 1～2，观察查询延迟、连接数和费用；
- 存储使用目标地域控制台允许的最低测试档，不要把其他地域或旧控制台展示的容量值当成固定承诺；
- 若追求调试时连接稳定，可保持最低 0.5 RCU；若把最低 RCU 设为 0 以启用自动暂停，必须接受暂停后的首次连接恢复延迟，并按照当前实例购买时间和控制台口径确认空闲判定时间；
- 本地连接池从 2～5 起步，短 `idleTimeout`，设置连接超时和有限重试。原文中的 `max: 5~10` 不应直接作为所有环境的固定值；长连接过多可能阻碍自动暂停并耗尽 Serverless 连接数；
- 本地访问 RDS 公网地址时，白名单只加入当前开发机公网出口 IP，禁止 `0.0.0.0/0`；家庭宽带或移动网络 IP 变化后需更新；
- 开启 SSL/TLS。生产应验证 CA 和主机名，不要仅通过 `sslmode=require` 加密而跳过证书校验。

连接串示意（仅给本地 API 使用，不能进入前端构建）：

```text
postgresql://nebula_app:<URL_ENCODED_PASSWORD>@<RDS_PUBLIC_HOST>:5432/nebula?sslmode=verify-full
```

注意：密码必须 URL 编码；生产同 VPC API 改用 RDS 内网 Host；数据库迁移账号与应用最小权限账号分离；连接串不得放进 `VITE_*`。

#### 14.4.3 OSS 凭证、Bucket 与 CORS

仅服务端可见的环境变量示意：

```text
OSS_REGION=oss-cn-...
OSS_BUCKET=nebula-dev-private
OSS_ENDPOINT=https://...
OSS_ACCESS_KEY_ID=<LOCAL_DEV_RAM_USER_ONLY>
OSS_ACCESS_KEY_SECRET=<LOCAL_DEV_SECRET_ONLY>
PRESIGN_TTL_SECONDS=300
```

必须满足：

- Bucket 为 private；浏览器预览/下载也使用短期 GET URL 或受控 API；
- 生产优先 RAM Role/STS 临时凭证；本地如必须使用 RAM 子用户，只授权测试 Bucket 和限定对象前缀，禁止使用主账号 AccessKey；
- `OSS_ACCESS_KEY_SECRET`、RDS 密码和连接串绝不能放入 `VITE_*`。Vite 环境变量会进入浏览器产物；`.env.local` 不能提交；
- object key 由服务端生成并绑定 workspace/asset，客户端不能自由指定任意路径；PUT/GET 签名 TTL 建议约 5 分钟，按文件大小和网络情况配置上限；
- CORS 精确允许 `http://localhost:5173` 和实际 HTTPS 测试域名；只开放需要的 PUT、GET、HEAD；Header 按上传契约收敛，至少处理 `Content-Type`；按排错需求暴露 `ETag`、`x-oss-request-id`；
- 不要使用任意 Origin、任意 Header 或公开 Bucket 规避 CORS；签名本身也不是权限校验替代品，签发前仍要校验登录态、workspace、类型、大小和频率。

#### 14.4.4 公网/内网连通矩阵

| 调用方 | RDS PostgreSQL | OSS |
|---|---|---|
| 本地 API | 公网地址 + 精确 IP 白名单 + SSL | 公网 Endpoint 或已验证的自定义域名 |
| 本地浏览器 | **禁止直连** | 仅使用后端签发的短期 PUT/GET URL |
| 生产同地域 API | VPC 内网地址 | 同地域 OSS 内网 Endpoint |
| 生产浏览器 | **禁止直连** | 短期签名 URL，经公网 Endpoint、自定义域名或 CDN |

“服务端 SDK 上传成功”不能替代“浏览器 CORS 直传成功”的验证。本地应分别测试 DNS、TLS、RDS 白名单、OSS CORS、签名过期、403、文件大小限制和跨域响应头。

### 14.5 完整性、功能可靠性与可用性保障

#### 14.5.1 页面—接口—数据矩阵

| 页面/能力 | 必须接入的真实能力 | 验收要求 |
|---|---|---|
| `/` 首页 | 今日计数、最近灵感、连续记录统计（若保留） | loading/empty/error/retry；统计日期口径明确 |
| `/capture` | 创建/更新草稿、标题/正文/类型保存、真实保存状态 | 返回稳定 ID 和版本；刷新后仍能恢复 |
| 左下角类型选择与内联管理 | 类型列表、详情、新增/重新启用、编辑、叉号归档；真正新建时后端随机颜色 | 无独立管理页/抽屉；系统类型无叉号且只读；自定义类型右侧叉号停用；归档不删历史灵感；同名重新添加恢复原 ID/slug/颜色且不新增行 |
| 附件 | presign、PUT、complete、失败重试、本地草稿恢复 | 主记录不因附件失败丢失；图片≤20MB；音频≤60秒；状态独立可见 |
| 灵感详情/编辑/删除 | 详情读取、修改原记录、软删除及确认反馈 | 编辑失败保留输入；删除 `202` 后各视图同步移除 |
| `/timeline` | 按日期查询、搜索、类型筛选、稳定 ID 多选、分页 | 不使用数组索引；0 条和分页结束有明确状态 |
| 白板 | 若承诺拖动，保存坐标/层级/版本 | 不持久化就明确为固定展示 |
| 星云视图 | 只能使用确定性规则或人工关系 | 不宣称 AI 聚类或 AI 标签融合已实现 |
| 预览/下载 | 短期 GET URL、过期续签、对象状态检查 | URL 不长期落库；未 ready 不展示成功 |

#### 14.5.2 可靠性验收门槛

至少应通过：

1. 保存后刷新、关闭浏览器再打开，数据可从 RDS 恢复；本地未同步草稿有明确 `local` 状态，不误报已同步。
2. 相同幂等键重复保存、重复提交、重复 `complete` 不产生重复记录。
3. 上传中断时主灵感仍保存；附件进入 `failed/pending`，可重试；失败路径仍保留可恢复的完整草稿。
4. `complete` 不能只凭 `{ asset_id }` 置为 ready。服务端必须 HEAD Object，核验对象存在、key 属于当前 workspace、大小、Content-Type 及可服务端验证的 ETag/CRC 等元数据；前端回传 SHA-256 不能单独作为可信完整性证明。
5. 跨 workspace 访问、更新、删除、签名请求返回 403/404，不泄露对象是否存在。
6. 删除采用软删除 + 待处理记录 + 幂等重试；模拟 OSS 删除失败后能够最终收敛，并可发现孤儿对象与悬空数据库记录。
7. 业务日期统一按 `Asia/Shanghai` 解释，数据库时间统一存 UTC；跨午夜、固定演示日期和空日期结果有测试。
8. RDS 自动暂停恢复、连接超时、OSS 403/CORS、签名过期、API 500、离线、弱网和 0 条数据均有非白屏、可操作的页面状态。
9. 手机端 HTTPS 下验证拍照、录音、后台切换和大文件上传；前后端对文件 MIME 和大小做一致的二次校验。
10. 本期不展示或不承诺 OCR、ASR、AI 标签自动融合结果；演示字段必须标注未启用。
11. 类型 CRUD 契约测试覆盖六色白名单、非灰色、颜色持久稳定、活动同名冲突、系统类型只读和归档不删历史灵感；另验证归档同名重新添加后 `archived_at=null`、记录数不增加且原 `id/slug/color_token` 不变。
12. 灵感详情、更新和删除契约测试覆盖多视图同步、软删除排除、`202` 响应和 OSS 删除补偿。
13. 完成数据库迁移前进/回滚测试、关键页面 E2E、私有 OSS 权限测试和备份恢复演练。

#### 14.5.3 监控与可用性

- 将 liveness、readiness 和 OSS dependency status 分开；OSS 短暂故障不应简单等同于进程死亡；
- 监控 API 成功率/p95、RDS 连接数/RCU/存储、Serverless 冷启动延迟、OSS 4xx/5xx、上传成功率、pending/failed 附件积压和删除重试次数；
- 日志携带 `request_id/workspace_id/inspiration_id/asset_id`，但不记录正文、AccessKey、数据库密码或完整签名 URL；
- 设定连接超时、有限指数退避、请求体大小限制和 workspace/IP 限流；
- “开启备份”不等于可恢复，必须把恢复到隔离实例并核对记录与附件引用作为上线前演练。

### 14.6 对本文原有方案的关键纠偏

1. **跨 RDS 和 OSS 没有单一数据库事务。** 原 §6.4/§13.8 的“事务级联 assets 行 + OSS 删除”应理解为业务流程，不是原子事务；推荐软删除、删除 outbox/任务记录、幂等重试和定期对账。
2. **EXIF 清理不是 RDS/OSS 自动能力。** 原 §9.1 的表述只有在前端重编码、服务端处理或已验证 OSS 图片处理链路存在时才成立；否则应披露原始 EXIF 风险。
3. **前端 SHA-256 不是可信证明。** 原 §13.8 的去重建议可作为提示或候选键，但服务端仍须核验对象存在、归属、大小、类型和状态；内容级去重后置。
4. **`complete` 需要服务端核验和状态机。** 不能只因收到 asset ID 就标记 ready；重复 complete 必须幂等，pending 超时对象需可清理。
5. **“无额外服务”不等于“无运行环境”。** 本板块无需队列/Worker/AI 网关，但 Express API 仍是不可省略的后端运行时。
6. **目标文档不等于当前代码已完成。** 类型详情/编辑/叉号停用、后端六色随机配色、已归档同名类型重新启用，以及前端灵感详情/编辑/删除闭环仍是当前明确缺口；目标 UI 必须复用左下角类型列表，不能以另建管理页替代。OCR/ASR/AI 演示文案也不能作为本期能力验收。

### 14.7 风险清单与 Go/No-Go

| 风险 | 等级 | 受限点 | 缓解措施 |
|---|---:|---|---|
| 无 Application API，只采购 RDS + OSS | **高** | 无鉴权、CRUD、签名、补偿和审计承载 | 增设 Express/API 运行时 |
| 中国内地 OSS 默认公共 Endpoint 限制 | **高** | 本地浏览器直传可能不可用 | 目标账号实测，准备自定义域名/代理/替代地域 |
| 页面 mock/假保存被当作真实后端 | **高** | 数据完整性和验收结论失真 | 页面—接口—数据库—E2E 矩阵 |
| 类型 CRUD、左下角叉号停用或同名重新启用不完整 | **高** | 无法可靠编辑/停用类型；归档同名重新添加可能产生重复类型；另建页面偏离现有交互 | 补齐详情/PATCH/DELETE；后端六色随机并持久化；POST 命中归档同名时恢复原记录；复用左下角列表且前端不传颜色 |
| 灵感内容后端接口存在但前端闭环缺失 | **高** | 用户无法可靠查看、编辑或删除已提交记录 | 统一详情组件、编辑表单、删除确认与多视图缓存同步 |
| DB/OSS 删除被误认为强事务 | **高** | 对象泄漏、悬空记录或误删 | 软删除、状态记录、重试、对账 |
| 长期 AccessKey/数据库密码进入前端 | **高** | 凭证泄露 | 服务端密钥、RAM/STS、最小权限 |
| `complete` 不做 HEAD/归属校验 | **高** | 伪造 ready、跨 workspace 绑定 | 服务端校验 key、workspace、size、type |
| 公网 RDS 白名单 `0.0.0.0/0` | **高** | 数据库暴露 | 精确出口 IP，生产内网，SSL |
| OSS CORS/签名权限过宽 | **高** | 任意来源上传或读取 | private Bucket、精确 Origin、短 TTL |
| Serverless 冷启动/连接池配置不当 | 中高 | 首次请求慢、连接耗尽 | 小连接池、超时、有限重试、监控 |
| RDS 形态/地域不满足高可用承诺 | 中高 | 备份、故障切换或 SLO 不成立 | 以控制台架构和 SLA 为准，必要时升级 |
| EXIF 清理被过度承诺 | 中高 | 隐私元数据泄露 | 明确真实处理链路或披露限制 |
| OCR/ASR/AI 占位与范围冲突 | 中 | 用户误解 | 隐藏、禁用或标注未启用 |
| 固定演示日期/伪统计 | 中 | 日期查询和首页统计错误 | UTC 存储、Asia/Shanghai 口径测试 |

**Go 条件：** API 运行时已确定；目标地域和 OSS 端点已实测；凭证、CORS、workspace 隔离和签名策略通过安全检查；类型完整 CRUD、左下角内联管理与叉号停用、后端六色随机配色、归档同名类型恢复原记录均通过验收；灵感详情/编辑/软删除及多视图同步通过验收；跨资源最终一致方案、`complete` 核验、附件失败重试和离线草稿均已实现。

**No-Go 条件：** 坚持只用 RDS + OSS 不部署 API；浏览器持有 RDS 连接串或 OSS 永久密钥；类型仍只能新增、固定灰色、另建管理页偏离现有左下角交互、删除会破坏历史灵感，或重新添加归档同名类型会产生重复记录；已提交灵感无法可靠查看/编辑/删除；页面以 mock/定时器假保存冒充落库；没有 workspace 隔离、备份恢复或孤儿对象清理机制；所选 RDS 形态达不到已承诺的可用性目标。

### 14.8 官方资料参考

- [RDS PostgreSQL Serverless instances](https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/serverless-instances)
- [Create a Serverless RDS PostgreSQL instance](https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/create-a-serverless-apsaradb-rds-for-postgresql-instance)
- [Configure an RDS PostgreSQL IP address whitelist](https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/configure-an-ip-address-whitelist-for-an-apsaradb-rds-for-postgresql-instance)
- [View and change RDS internal and public endpoints and ports](https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/view-and-change-the-internal-and-public-endpoints-and-ports-of-an-apsaradb-rds-for-postgresql-instance)
- [OSS regions and endpoints](https://www.alibabacloud.com/help/en/oss/user-guide/regions-and-endpoints)
- [Upload an OSS object by using a signed URL](https://www.alibabacloud.com/help/en/oss/developer-reference/upload-an-object-by-using-a-signed-url)
- [Configure OSS CORS](https://www.alibabacloud.com/help/en/oss/user-guide/cors)
- [Use STS temporary access credentials to access OSS](https://www.alibabacloud.com/help/en/oss/developer-reference/use-temporary-access-credentials-provided-by-sts-to-access-oss)
- [Map custom domain names to OSS](https://www.alibabacloud.com/help/en/oss/user-guide/map-custom-domain-names)

---

**本专项最终结论：** 对当前 `Nebula-React` 的灵感记录范围，采用“RDS PostgreSQL Serverless + private OSS + Express Application API”是可行的；单独依赖 RDS 与 OSS 则不可行。中国内地 OSS 公网端点限制、当前前端 mock/假保存、跨资源删除一致性和浏览器凭证暴露是上线前最高优先级风险。

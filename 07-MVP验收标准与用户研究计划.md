# MVP 验收标准与用户研究计划

## 1. 目的

该文档把“方案合理”转换为可验证结果。MVP 的成功不是页面全部完成，而是目标用户能在可控风险下把真实灵感转化为双平台内容，并理解每一步发生了什么。

## 2. MVP 端到端验收场景

### 场景 A：记录多模态灵感

前置：已登录 Web 工作台。

步骤：

1. 点击首页“记录灵感”；
2. 输入文字，上传 2 张图片和 1 个音频；
3. 跳过类型选择并保存；
4. 离开页面；
5. 稍后回到当日白板；
6. 查看图片与音频转写状态；
7. 修改转写错误并接受一个 AI 建议类型。

验收：

- 至少一种内容即可保存；
- 上传进度逐文件可见；
- 原件保存不依赖 ASR/OCR 成功；
- 处理完成后无需刷新整个页面；
- 用户可区分原文、AI 建议和已确认字段；
- 删除原件时明确说明派生数据处理。

### 场景 B：按日期选择灵感

步骤：

1. 点击“开始创作”；
2. 滚动年、月、日；
3. 最终确认一个真实单日；
4. 在日白板选中 2–3 张贴片；
5. 进入素材篮。

验收：

- 闰年、月份天数正确；
- 有灵感日期有数量提示；
- 键盘和直接输入日期均可完成；
- 多选结果和返回后的选择状态正确；
- 贴片不会完全遮挡，整理操作可撤销。

### 场景 C：灵感 + 热点生成选题

步骤：

1. 从 AI 热点选择一个条目；
2. 查看来源、抓取时间和原始标题；
3. 与已有灵感组合；
4. 查看相关性分析；
5. 即使相关性低也继续；
6. 修改并确认一个候选选题。

验收：

- 热点失败时可显示上次缓存和时间；
- 相关性含维度、理由、冲突与置信度；
- 分数不是硬阻断；
- 选题记录 Evidence 引用；
- 用户可完全自定义选题。

### 场景 D：从选题到核心内容骨架

步骤：

1. 确认一个候选选题；
2. AI 从素材篮生成“创作要点”；
3. 修改目标读者、核心主张和关键观点；
4. 查看并修正 Evidence 引用；
5. 将一个不确定数字标记为暂不使用；
6. 锁定一个观点并重新生成新版本。

验收：

- `ContentCore` 包含主题、受众问题、承诺、主张、Evidence、案例、素材和约束；
- 不包含完整公众号文章、小红书正文、平台标题或分镜；
- 锁定内容不被覆盖；
- 系统创建版本而非破坏旧内容；
- 每个关键事实和主张可查看来源；
- 超出上下文预算时显示被裁剪素材；
- AI 不可用时仍可手工编辑。

### 场景 E：小红书原生创作

步骤：

1. 从已确认的 `ContentCore` 选择小红书笔记策略；
2. 生成并确认标题候选、开头钩子和图文分镜；
3. 将一张原图拖入指定卡片；
4. 修改某一页卡型和卡片文案；
5. 锁定后重新生成另一页；
6. 生成短正文与话题并检查溢出；
7. 导出 PNG/ZIP 发布包，包含图片、正文、话题、manifest 和来源说明。

验收：

- 分镜不是按完整长文或公众号 H2 机械切图；
- 导出为 1080×1440，页序、预览和导出一致；
- 锁定页不变；
- 溢出来自真实渲染测量；
- 原图来源、缺图、低分辨率是可解释提醒；
- 导出失败不破坏已编辑分镜；
- 平台稿记录 `sourceCoreVersion`。

### 场景 F：公众号原生创作与草稿箱

步骤：

1. 从同一个 `ContentCore` 创建公众号分支；
2. 生成并调整公众号文章大纲；
3. 分节成稿、锁定一节并完成全文编辑；
4. 连接公众号并探测能力；
5. 将公众号稿转换为兼容 HTML；
6. 上传正文图片和封面；
7. 预览并确认后创建草稿。

验收：

- 公众号大纲和文章稿独立于小红书分镜；
- AppSecret 不到浏览器，token 不出现在普通日志；
- 实际能力检测结果可见；
- 排版层不从 `ContentCore` 擅自补写完整文章；
- 图片按哈希复用；
- 官方错误码转换为可操作提示；
- 成功记录草稿标识、`ContentCore` 版本和公众号稿版本；
- MVP 不自动群发。

### 场景 G（P1 灰度）：小红书实验性发布

该场景用于 Phase 4/P1 灰度，不作为核心 MVP 上线阻断条件；MVP 的小红书交付底线是可靠导出发布包。

步骤：

1. 安装/连接本地发布桥；
2. 检查登录状态；
3. 查看完整发布内容；
4. 用户确认；
5. 本地桥执行并回传结果。

验收：

- Cookie 不上传云端；
- 用户看到实验性风险；
- 确认后 payload 由哈希保护；
- 不确定是否成功时不自动重复发布；
- 本地桥不可用时可下载发布包；
- 用户可取消且不会静默发布。

## 3. 功能验收矩阵

| 模块 | P0 阻断问题 | P1 可延期问题 |
|---|---|---|
| 灵感记录 | 丢原件、保存假成功、无法恢复 | AI 标签准确率一般 |
| 日期白板 | 日期错误、选择丢失、严重遮挡 | 自由布局不够丰富 |
| 热点 | 无来源、旧数据冒充实时 | 聚类效果一般 |
| 相关性 | 无解释、硬阻断 | 分数校准不完美 |
| 核心内容骨架 | 事实无来源、覆盖锁定内容、无法恢复版本 | AI 归纳不够个性化 |
| 小红书原生创作 | 分镜机械切文、预览与导出不一致、图片丢失 | 模板数量较少 |
| 公众号 | 泄露密钥、错误草稿、图片失效 | 主题数量较少 |
| 发布 | 重复发布、未经确认发布 | 诊断信息不够精细 |

## 4. 质量指标初值

这些是团队目标，不是对平台算法的宣称：

- 灵感保存成功率 ≥ 99.5%（排除用户网络完全中断）；
- 已接收文件的原件丢失率为 0；
- 异步任务最终状态可解释率 ≥ 99%；
- 日期选择与时区测试通过率 100%；
- 卡片预览/导出视觉回归通过率 ≥ 98%；
- 发布任务重复提交事故为 0；
- 公众号连接错误中，≥ 95% 可映射到用户可操作提示；
- 用户研究中 ≥ 80% 参与者无需帮助完成“记录→选题”；
- ≥ 70% 参与者能正确解释核心内容骨架与平台稿的关系，并知道修改核心不会静默覆盖平台稿。

正式阈值应在试运行后根据样本量调整。

## 5. 用户研究计划

### 5.1 招募

首轮 5–8 人，至少覆盖：

- 同时运营公众号与小红书；
- 每周创作 ≥1 次；
- 有语音/截图/备忘录等多种素材；
- 个人创作者与小团队；
- AI 深度用户和谨慎用户；
- 愿意自动发布和只愿导出的两类用户。

第二轮灰度 10–20 人，连续使用 2–4 周。

### 5.2 访谈问题

避免问“你喜欢这个功能吗”，重点问真实行为：

1. 最近一次灵感从哪里来，如何保存？
2. 最近一次没写成的内容卡在哪里？
3. 你如何决定一个热点值不值得跟？
4. 同一个主题发两个平台时，哪些部分一定不同？
5. 你曾经为何放弃某个 AI 生成结果？
6. 图片通常是原图、截图、设计卡片还是 AI 图？
7. 发布前必须检查什么？
8. 你是否愿意连接账号？什么情况只愿导出？

要求参与者展示最近一次真实工作流，在获准和脱敏后记录步骤。

### 5.3 原型任务

- 在 30 秒内记录语音 + 图片灵感；
- 找到上周某一天的灵感；
- 选择两个看似不相关的灵感；
- 判断一条热点是否值得结合；
- 修改 AI 给出的选题而不是照单全收；
- 从同一核心内容骨架创建结构明显不同的小红书与公众号版本；
- 处理一张不适合直接发布的图片；
- 面对实验性小红书连接做出发布或导出选择。

观察：完成率、耗时、犹豫点、误解、返回行为和用户原话。

### 5.4 需要验证的争议点

#### 日期白板

假设：日期能触发情境记忆。  
风险：长期用户更依赖搜索和主题。  
验证：比较“按日期找到素材”和“搜索/标签找到素材”的成功率与主观负担。

#### 相关性分数

假设：分维度解释能减少盲目追热点。  
风险：精确分数让用户误以为是权威判断。  
验证：A 组显示数值 + 解释，B 组只显示高/中/低 + 解释。

#### 共享核心内容模式

假设：共享事实、观点、Evidence 和素材可以减少重复劳动，同时让两个平台保持原生表达。  
风险：核心内容骨架过于抽象，成为新的表单负担；或用户误以为两个平台标题、顺序和语气也应同步。  
验证：比较“完整通用长文再适配”和“AI 预填核心内容后分流”两种原型的完成率、犹豫点和重写比例；确认创作要点页面无需用户从空白填写。

记录指标：两个平台对核心主张/Evidence 的复用率，以及标题、结构、段落、分镜、CTA 的独立改写率。

#### AI 生图

假设：用户更需要模板信息卡和封面，而非重绘生活素材。  
验证：访谈选择题结合真实任务，不只问意愿；统计实际使用。

#### 自动发布

假设：公众号草稿箱价值高，小红书更倾向导出或本地确认。  
验证：观察真实连接率、草稿创建率、发布包导出率和放弃原因。

## 6. 埋点与反馈

事件只记录必要元数据，不记录敏感正文：

- `inspiration_created`：模态组合、是否填写类型；
- `daily_board_opened`：日期距离今天的天数；
- `inspiration_selected_for_creation`；
- `hotspot_attached`：新鲜度、来源；
- `relation_feedback`：有帮助/没关系/意外但有趣；
- `topic_confirmed`：生成/自定义/混合；
- `content_core_edited`：字段、Evidence、锁定和版本变化；
- `platform_draft_created`：platform、sourceCoreVersion、生成策略；
- `xhs_storyboard_edited` / `wechat_outline_edited`；
- `platform_draft_sync_prompted/accepted/dismissed`；
- `platform_rendered`；
- `publication_validated/confirmed/succeeded/failed`；
- `export_package_downloaded`。

所有事件使用项目内匿名 ID；禁止把 AppSecret、Cookie、完整正文或原音频写入分析事件。

## 7. 灰度退出条件

满足以下条件才扩大用户范围：

- 连续两周无原件丢失；
- 无未经确认发布；
- 无重复发布事故；
- 公众号主要错误可诊断；
- 若进入 Phase 4/P1 灰度，小红书桥风险提示和手工退路有效；
- 至少 10 个真实项目完成核心内容骨架和至少一个平台原生稿；
- 至少 5 个项目从同一核心内容骨架完成两份平台原生稿；
- 核心更新后的 `stale` 提示、差异预览和锁定内容保护经过真实任务验证；
- 用户能理解热点来源和 AI 摘要差异；
- 团队已处理 P0 问题并评审剩余风险。

## 8. 是否继续投入的判断

MVP 后建议用证据决定方向：

- 若记录多、转化低：优先改善检索、回顾和素材推荐；
- 若选题确认多、核心内容确认少：优先降低“创作要点”的表单感并改善 AI 预填；
- 若核心内容确认多、平台成稿少：分别定位小红书分镜或公众号大纲/分节生成的摩擦；
- 若核心更新后同步放弃高：优先改善差异解释和新版本选择，不急于做自动合并；
- 若导出多、自动发布少：不要强推自动化，完善发布包；
- 若用户主要使用单平台：重新评估“双平台”是否应为默认而非可选；
- 若 AI 图使用极低：将投入转向原图处理和模板卡片。

## 9. 上线核验清单

- [ ] 微信官方接口、账号权限、字段和限制已按上线日期重新核验
- [ ] AIHot 使用条款、服务稳定性、限流和数据来源已确认
- [ ] `xiaohongshu-mcp` 当前版本已完成独立风险测试
- [ ] 第三方许可证和复用代码已审查
- [ ] 密钥、Cookie、日志和对象存储安全评审通过
- [ ] 删除、导出和隐私说明可用
- [ ] 发布前确认不可绕过
- [ ] 幂等、失败恢复和审计日志完成
- [ ] 手工导出退路完成
- [ ] 用户研究中的 P0 误解已修正

## 10. 最终验收定义

MVP 被认为“可用”，需同时满足：

1. 用户可无阻塞记录多模态灵感；
2. 可按单一日期回到当日白板并选择灵感；
3. 热点有来源、时间和快照，LLM 不伪装成实时数据源；
4. 用户能确认选题和核心内容骨架，关键事实可追溯；
5. 同一 `ContentCore` 可创建结构不同、独立可编辑的小红书原生稿和公众号原生稿；
6. 核心内容更新后平台稿显示 `stale` 和差异，不静默覆盖人工编辑；
7. 小红书至少能可靠导出 PNG/ZIP 发布包；本地自动执行属于 P1/Phase 4 实验连接，不作为 MVP 阻断项；
8. 公众号可在实际有权限的测试账号中创建草稿；
9. 所有发布都经过明确确认，失败不重复发布；
10. 关键内容可追溯、可恢复、可删除；
11. 至少一轮真实创作者测试证明流程比现有做法更顺畅。

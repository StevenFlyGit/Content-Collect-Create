# 交互流程与视觉设计规范

## 1. 视觉方向：有秩序的灵感宇宙

四张参考图中，图一适合承担桌面 Web 的信息骨架，图二适合赋予产品情绪和辨识度。推荐不是简单做“浅色页 + 深色页”，而是把两者分工：

- **图一提供秩序**：奶油白、低对比网格、大留白、纸片、明确层级；
- **图二提供生命力**：低饱和星云光晕、微光点、磨砂层、缓慢漂移；
- **图三提供未来探索隐喻**：语义星云作为 P2 辅助视图；
- **图四提供热点雷达的信息骨架**：来源、刷新状态、热点卡片与进入创作。

设计关键词：`calm / airy / tactile / traceable / focused`。

## 2. 不建议的做法

- 整个编辑器长期使用高对比深色星云背景；长文编辑会疲劳。
- 让贴片无限旋转、自由堆叠；这会牺牲可扫读性。
- 每个类型使用高饱和颜色；类型越多越混乱。
- 用大量玻璃拟态降低正文对比度。
- 所有操作都隐藏在 hover；触屏和键盘用户无法发现。
- 仅用颜色表示类型、选择或错误。

## 3. 设计系统基础

### 3.1 色彩

建议使用 CSS 变量，支持未来主题切换：

```css
:root {
  --canvas: #f7f3ea;
  --surface: #fffdf8;
  --surface-glass: rgba(255, 253, 248, 0.78);
  --ink: #25231f;
  --ink-muted: #6f6a60;
  --grid: rgba(74, 69, 61, 0.07);
  --line: rgba(74, 69, 61, 0.14);
  --accent-violet: #7867a8;
  --accent-blue: #6f91aa;
  --accent-rose: #b77b83;
  --accent-moss: #788a6b;
  --focus: #4f64c8;
  --danger: #b84a4a;
  --success: #4f7f62;
}
```

星云光晕只使用 5%–14% 的低透明度径向渐变，不能影响文字对比度。贴片颜色建议限制为 5 个低饱和族，并叠加类型图标/文字标签。

### 3.2 字体和排版

- 中文正文优先系统无衬线字体，确保性能和跨平台一致性；
- 品牌大标题可使用较有性格的衬线或展示字体，但仅限短文本；
- 正文基准 16px，行高 1.65–1.8；
- 编辑器内容宽度约 680–760px；
- 信息卡正文不低于 14px；
- 任何关键文本对比度满足 WCAG AA。

### 3.3 圆角、阴影、材质

- 面板圆角：16–24px；
- 贴片圆角：12–16px；
- 阴影以扩散、低透明为主，不使用重黑投影；
- 纸片用轻微纹理，玻璃卡只在浮层/选中态使用；
- 选中态除了描边，还显示复选标记与数量。

### 3.4 动效

- 页面主转场：180–260ms；
- 星点漂移：20–40 秒循环，位移不超过 12px；
- 贴片进入：淡入 + 4–8px 位移；
- 禁止持续旋转和强视差；
- 遵循 `prefers-reduced-motion`，关闭非必要动效。

## 4. 首页构图

```text
┌────────────────────────────────────────────────────────────┐
│ 品牌                          灵感库 热点雷达 项目   头像   │
│                                                            │
│        把散落的灵感，慢慢变成可以分享的作品。               │
│        今日已收集 3 条灵感                                  │
│                                                            │
│   ┌──────────────────┐    ┌──────────────────┐             │
│   │   ＋ 记录灵感     │    │   ✦ 开始创作     │             │
│   │  文字/图片/音频   │    │  从某一天出发    │             │
│   └──────────────────┘    └──────────────────┘             │
│                                                            │
│   最近项目                         轻量星云光晕/网格背景      │
└────────────────────────────────────────────────────────────┘
```

两个主按钮面积和层级相等，不应暗示其中一个是次要操作。按钮卡片内部可用图一式纸片秩序，卡片外部用图二式光晕连接。

## 5. 灵感记录交互

### 5.1 快速记录面板

建议从底部或右侧升起，不离开用户当前上下文：

```text
[写下此刻的想法……]
[＋图片] [＋音频] [类型：先不分类 ▼] [标签]

附件区：缩略图 / 音频条 / 上传进度

                         [存为草稿] [记录灵感]
```

细节：

- 文本框打开后立即聚焦；
- 粘贴图片自动成为附件；
- 拖拽文件有明确投放区域；
- 未完成上传时可保存本地草稿，但不能误报“已同步”；
- 类型选择器位于面板左下角，首项是“先不分类”，底部只提供“新建自定义类型”；类型的查看和删除/停用均在展开列表内完成，不新增独立管理页面、抽屉或“管理类型”入口；
- AI 类型建议在异步处理后出现，用户一键接受或忽略；
- 离开时若存在未保存内容，提供恢复而非强阻断弹窗。

### 5.2 处理反馈

每个附件独立显示：上传、转写/OCR、完成、失败。不要用一个全局进度掩盖部分失败。

### 5.3 灵感类型选择与管理

目标 UI 组件及职责：

| 组件 | 职责 |
|---|---|
| `TypeSelect` | 位于快速记录面板左下角；展开后展示系统初始类型和当前工作区活动自定义类型，并使用 `color_token` 渲染选项左侧圆圈 |
| `TypeOptionRow` | 将类型选中区域与删除操作区域分离；仅活动的工作区自定义类型在名称右侧显示小型叉号，系统初始类型不显示叉号 |
| `TypeCreateForm` | 位于列表底部“新建自定义类型”流程中；输入自定义类型名称和可选图标，不提供任意颜色输入框 |
| `TypeArchiveConfirmPopover` | 在当前列表上下文中轻量确认“仅停止后续选择，不删除已有灵感”；不构成独立管理页或管理抽屉 |

交互规则：

1. 当前类型管理界面就是图示左下角展开的类型选择列表，不新增独立类型管理页面、`TypeManagerDrawer` 或额外“管理类型”入口。
2. 真正首次新建自定义类型时，前端只提交 `label` 和可选 `icon`，颜色由后端从 `想法`、`引用`、`随感`、`待办`、`案例`、`问题`的六个颜色 token 中随机分配。
3. 创建请求进行中可显示加载骨架，但不得先用灰色圆圈作为新类型的最终视觉；成功后必须以接口返回的 `color_token` 立即渲染圆圈并选中新类型。
4. 随机颜色一经返回即为该类型的稳定颜色。重新打开选择器、编辑名称或刷新页面时不得重新随机；编辑名称后圆圈颜色保持不变。
5. 每个活动的工作区自定义类型名称右侧显示小型叉号。叉号点击区域与类型选中区域必须分离，点击叉号不得同时选中该类型；系统初始类型无删除入口。
6. 删除使用“停用/归档”语义。确认成功后该类型从活动选择列表中移除；引用它的既有灵感仍显示原名称与颜色，并附加“已停用”弱提示。若被停用类型正处于当前未提交记录的选中状态，选择值回退为“先不分类”，并给出轻提示。
7. “重新添加已删除类型即重新启用”：用户在“新建自定义类型”中输入当前工作区已归档类型的同名名称时，后端恢复原记录而不新建；前端使用返回的原 `id`、`slug` 和 `color_token` 将其重新加入活动列表，不重新随机颜色，并提示“已重新启用该类型”。
8. 活动同名仍显示名称冲突；只有已归档同名执行重新启用。权限不足和网络失败均在当前列表或表单附近显示可操作错误，不关闭类型选择器，不丢失用户输入。

六色池与初始类型保持一一对应：`--nebula-violet`（想法）、`--nebula-blue`（引用）、`--nebula-rose`（随感）、`--nebula-mint`（待办）、`--nebula-amber`（案例）、`--danger`（问题）。`--ink-muted` 仅用于“先不分类”，不得用于自定义类型的默认创建结果。

### 5.4 灵感库：卡片交互、详情查看与编辑删除

灵感库以网格/列表展示灵感卡片。本小节约定卡片上的入口布局、查看/编辑弹框形态、删除确认流程，以及与创作流程的衔接。

#### 5.4.1 组件职责

| 组件 | 职责 |
|---|---|
| `InspirationLibraryCard` | 在灵感库网格/列表中展示单条灵感摘要；承载选中、查看、编辑、删除入口 |
| `InspirationDetailModal` | **PC 端居中弹框**（移动端可用底部 Sheet），只读展示灵感的完整标题、正文、类型、发生时间、语音/图片素材 |
| `InspirationEditModal` | **PC 端居中弹框**（移动端可用底部 Sheet），复用创建灵感页面的样式与交互，用于二次编辑 |
| `InspirationDeleteConfirmModal` | 屏幕居中确认弹框，以文艺文案提示删除后果，要求二次确认 |

#### 5.4.2 卡片交互与状态

```text
┌─────────────────────────────────────┐
│  [×]                    14:33 [编辑] │  ← 左上角删除按钮，右下角编辑按钮
│                                     │
│   [想法]                            │
│   “这一刻，我终于能完成              │
│    第一条记录”                       │
│                                     │
│   🖼 2   🎙 01:24                   │
└─────────────────────────────────────┘
```

交互规则：

1. **删除入口**：将原卡片上的勾选框替换为「带叉号的删除图标按钮」，位于卡片**左上角**（桌面端）或卡片头部左侧（移动端）。点击后弹出居中删除确认框，不直接删除。
2. **编辑入口**：在卡片**右下角**嵌入「编辑」按钮（文字或铅笔图标），hover / 键盘焦点时显现，移动端默认显示。点击后打开 `InspirationEditModal`。
3. **查看入口**：移除卡片上现有的「查看」按钮。改为**双击卡片任意非按钮区域**打开 `InspirationDetailModal`。
4. **选中用于创作**：**单击卡片任意非按钮区域** = 选中/取消选中该灵感，用于后续"用这些灵感创作"。选中态显示清晰描边与勾选标记，标记位置建议放在卡片左下角或类型标签旁，不占用删除按钮位置。
5. 删除按钮、编辑按钮与卡片主体点击区域必须分离；点击删除/编辑按钮不得触发选中或查看。
6. 多选时底部出现"用这些灵感创作"操作栏，不弹出遮挡列表的模态框。

#### 5.4.3 删除确认弹框

删除是高危操作，必须二次确认。弹框在屏幕正中央，带背景遮罩。

**文案备选（文艺感，4 个版本）：**

| 版本 | 文案 |
|---|---|
| A | "灵感来之不易，建议勿要丢弃。删除后将会消散在你的灵感星云之中。" |
| B | "这条灵感曾在某一刻照亮过你，确定要让它熄灭吗？" |
| C | "一旦删除，它将从列表、白板与搜索中隐去，像流星划过不再回头。" |
| D | "你正把一段思绪放回宇宙。确认后，它将在星云深处慢慢消散。" |

弹框结构：

```text
┌────────────────────────────────────────────┐
│  ✕                                         │
│                                            │
│   "灵感来之不易，建议勿要丢弃。             │
│    删除后将会消散在你的灵感星云之中。"       │
│                                            │
│        [ 取消 ]      [ 确认删除 ]            │
└────────────────────────────────────────────┘
```

设计细节：

- 标题可省略，仅用文案占据视觉中心；
- "确认删除"使用危险色（`--danger`），"取消"使用次级按钮样式；
- 弹框宽度 PC 端 400–440px，移动端占屏宽 85%–92%；
- 点击遮罩或按 `Esc` 等同于"取消"；
- 确认后进入删除中状态；接口受理成功后关闭弹框并从列表、白板、搜索结果中移除卡片；失败保留卡片并显示错误提示。

#### 5.4.4 查看弹框（只读）

双击卡片打开 `InspirationDetailModal`，PC 端居中弹出。

```text
┌────────────────────────────────────────────┐
│  第一条图片灵感                ✕            │
├────────────────────────────────────────────┤
│  ● 啪啪啪啪                                 │
│  发生时间：2026/8/20 14:33:04              │
│                                            │
│  （无正文）                                  │
│                                            │
│  [语音] 01:24                              │
│                                            │
│  [图片 1 原尺寸] [图片 2 原尺寸]             │
│                                            │
│  同步状态：已同步                             │
│  创建时间：2026/8/20 16:15:46               │
│  更新时间：2026/8/20 16:15:47               │
├────────────────────────────────────────────┤
│        [ 编辑 ]      [ 删除 ]              │
└────────────────────────────────────────────┘
```

设计细节：

- 弹框最大宽度 720px，最大高度 85vh，内容超出时内部滚动；
- 顶部标题左对齐，右侧关闭按钮；
- 类型标签带颜色圆点，与创建页类型选择器一致；
- 发生时间完整显示日期 + 时间；
- 正文为空时显示"（无正文）"弱提示；
- 语音素材显示播放条/时长，点击可播放；
- 图片素材按**原尺寸与原始顺序**展示，不强制裁剪为统一高度；宽度不足时允许横向滑动或纵向堆叠；
- 底部"编辑"使用主按钮样式，"删除"使用危险文字/按钮样式。

#### 5.4.5 编辑弹框

点击卡片右下角「编辑」按钮打开 `InspirationEditModal`，PC 端居中弹出，**复用创建灵感页面的样式与交互**。

```text
┌────────────────────────────────────────────┐
│  编辑灵感                      ✕            │
├────────────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐   ┌───────┐      │
│  │ 2025│ │ 07  │ │ 19  │   │ 14:33 │      │
│  │>2026<│ │>08< │ │>20< │   │       │      │
│  │ 2027│ │ 09  │ │ 21  │   │       │      │
│  └─────┘ └─────┘ └─────┘   └───────┘      │
│   年      月      日        时间             │
├────────────────────────────────────────────┤
│  [可滚动内容区]                              │
│                                            │
│  标题                                       │
│  ┌──────────────────────────────────┐     │
│  │ 第一条图片灵感                      │     │
│  └──────────────────────────────────┘     │
│                                            │
│  正文                                       │
│  ┌──────────────────────────────────┐     │
│  │                                  │     │
│  │                                  │     │
│  └──────────────────────────────────┘     │
│                                            │
│  类型：啪啪啪啪 ▼                           │
│                                            │
│  [语音] 01:24  [播放] [替换] [删除]          │
│                                            │
│  [图片 1 原尺寸] [图片 2 原尺寸]             │
│                                            │
├────────────────────────────────────────────┤
│        [ 取消 ]      [ 保存修改 ]            │
└────────────────────────────────────────────┘
```

设计细节：

- **顶部日期选择**：复用灵感库顶端日期滚轮组件，但适当紧凑（行高、字号、间距缩小约 20%）；三列（年/月/日）并排，右侧留出时间选择位（HH:mm 输入或滚轮）。
- **内容区可滚动**：当标题、正文、素材较多时，内容区独立滚动，底部操作栏固定在弹框底部。
- **字段顺序**：从上到下依次为：标题、正文、类型选择、语音素材、图片素材。
- **图片展示**：按原尺寸与原始顺序展示，不强制统一高度；在弹框宽度不足时允许横向滑动或纵向堆叠。
- **语音素材**：显示当前语音时长与播放控制，允许替换或删除。
- 底部"取消"和"保存修改"按钮与创建页底部按钮样式一致；"保存修改"在提交期间禁用，防止重复提交。
- 未保存离开时提示保存草稿或放弃变更。
- 若原类型已停用，编辑表单允许保留原值，也允许改选有效类型；一旦改选并保存，已停用类型不再出现在选择器中。

#### 5.4.6 与创作流程的衔接

1. 单选/多选：单击卡片主体进入选中态，底部出现"用这些灵感创作"操作栏。
2. 查看中创作：在查看弹框底部点击"编辑"可进入编辑弹框；编辑保存后同步更新列表、白板和搜索结果中的同一条灵感。
3. 删除后联动：确认删除并从列表移除后，同步移除白板贴片与搜索结果中的同一条灵感；附件后台清理不阻塞界面。
4. 更新或删除失败时保留弹框和用户输入，显示明确错误；不得提前移除卡片或显示成功。

### 5.5 录音音频附件卡片交互

录音（语音）是核心输入方式之一。为避免语音卡片被图片网格挤成窄条、导致控件拥挤与进度条难操作，音频附件在附件区中应作为**横向全宽卡片**独立呈现，与图片缩略图网格分离。本小节对应落地方案 A：聚焦布局放宽与控件放大，低成本解决现有痛点；自定义进度条（方案 B）作为后续可选升级在 5.5.4 中预留。

#### 5.5.1 布局模型

附件区由两部分构成，音频卡片独占一行，图片以单行横向排列：

```text
附件区：
┌──────────────────────────────────────────────────┐  ← 音频卡片（独占一行，全宽）
│  ▶ / ⏸   ┃▁▂▃▅▆▃▂▁░░░░░░░░░░░░░░┃   00:10 / 00:32  │
│  [语音] 00:32   本地草稿                    [×]    │
└──────────────────────────────────────────────────┘
┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ →（横向滚动）       ← 图片单行排列
│ img │ │ img │ │ img │ │ img │
└─────┘ └─────┘ └─────┘ └─────┘
```

- **音频卡片**：脱离 `auto-fill` 网格，使用 `display: flex; width: 100%`，在附件区中单独占满一行，不再受 `minmax(120px, 1fr)` 列宽挤压。
- **图片排列**：使用 `display: flex; overflow-x: auto` 的单行横向容器，多张图片以固定尺寸缩略图顺序排布并可横向滚动查看，不换行为网格；容器宽度占满附件区。图片的顺序即附件数组顺序，可作为拖拽排序的基础（见 5.6）。

#### 5.5.2 信息分区

音频卡片内部分为三个清晰分区，避免窄框下的压迫感与层级混乱：

| 分区 | 内容 | 说明 |
|---|---|---|
| 控制区 | 播放 / 暂停按钮 | 最小 40×40px，与进度区、元数据区间距 ≥12px |
| 波形 / 进度区 | 波形条 + 进度指示 | 高度 48–64px；当前为静态装饰波形，后续可升级为真实波形 |
| 元数据区 | 类型圆点、时长、上传状态、删除按钮 | 底部一行，弱对比文字，不与控制区重叠 |

#### 5.5.3 交互规则

1. **控件放大**：播放 / 暂停按钮最小 40×40px，控件之间间距 ≥12px，降低窄框下的拥挤感。
2. **移动端全宽**：语音卡片在移动端建议全宽独占一行，不挤入图片网格；移动端无 hover，控件必须始终可见且足够大，便于触控。
3. **拖动手画暂停**：用户拖动进度条（或波形区域）时，暂停波形装饰动画（`animation-play-state: paused`），释放后恢复，降低视觉干扰。
4. **状态联动**：上传中 / 失败 / 已上传的 `att-status` 与播放控件联动——上传失败态禁用播放控件，已上传态允许播放，本地草稿态可本地预览播放；不得用单一颜色传达状态，需配合文字标签。
5. **删除入口**：复用附件区统一的删除按钮（卡片右上角 ×），点击调用 `removeAttachment` 移除附件；删除按钮与播放 / 进度区点击区域分离，避免误触。
6. **可达性基线**：方案 A 复用浏览器原生 `<audio>` 控件的键盘能力（Tab 聚焦、方向键微调、Space 播放 / 暂停），满足第 13 章“所有主操作可由键盘完成”的底线。

#### 5.5.4 后续升级（方案 B，可选）

若需完全掌控进度条交互，可升级为自定义播放器，届时必须补齐：

- 进度条使用 `role="slider"`、`aria-valuenow / aria-valuemin / aria-valuemax`、`aria-label="播放进度"`；`←/→` 微调进度（建议 ±5s），`Space` 播放 / 暂停。
- 拖动时显示时间气泡，已播放段用品牌紫、未播放段用中性灰、缓冲段用半透明紫，缓冲状态除颜色外另用文字标签或脉冲骨架表达。
- 波形从固定装饰数组升级为后端生成的真实振幅图，播放时高亮已播放段。
- 可增设 ±5s / ±15s 快进快退按钮，但短语音（≤1 分钟）非必需。

### 5.6 附件拖拽排序

记录灵感时支持插入图片（本地选择 / 拍照）与录音（见 5.5）。为提升整理效率，附件支持拖拽调整顺序。本节定义拖拽交互、视觉反馈与可达性要求，与 5.4.2 的卡片点击行为互不冲突。

#### 5.6.1 图片拖拽排序

1. **单行横向容器**：图片在 5.5.1 定义的单行横向容器中展示；长按或拖拽任意缩略图即可拖动，松手后定序。
2. **视觉反馈**：被拖拽项半透明或抬升；插入位置以空隙或指示线提示，避免与其它附件混淆；拖动期间不改变音频卡片位置。
3. **顺序持久化**：松手后，图片的新顺序写入当前草稿的附件数组顺序，随草稿保存与提交一并生效；后端按数组顺序渲染，不额外存储独立排序字段。
4. **触控适配**：移动端无 hover，长按 200–300ms 进入拖拽态，避免与单击 / 双击冲突；拖拽过程提供跟随手指的缩略图或占位。
5. **可达性**：除拖拽外，提供键盘重排入口（选中图片后方向键调整位置，或提供上移 / 下移按钮），满足第 13 章“所有主操作可由键盘完成”的底线；拖拽手柄需有 `aria-label`。

#### 5.6.2 音频卡片拖拽（可选）

若实现成本可控，音频卡片在独占一行的基础上，也支持与图片同等粒度的拖拽排序：

- 音频卡片与图片缩略图处于同一可排序附件序列，拖拽音频卡片可在图片之间插入或调整位置。
- 拖拽交互、视觉反馈、顺序持久化与可达性要求与 5.6.1 保持一致。
- 若成本较高，可暂缓此项，仅图片支持拖拽；音频卡片默认保持“先音频后图片”的展示顺序，不阻塞基础记录流程。

#### 5.6.3 边界与约束

- 拖拽仅调整展示与存储顺序，不修改原始文件；删除中 / 上传中 / 失败的附件在排序时保持稳定，防止状态错乱。
- 拖拽过程不得触发卡片的查看 / 编辑 / 选中（见 5.4.2）等其它点击行为；拖拽手柄与播放 / 删除操作区点击区域分离。
- 在 `prefers-reduced-motion` 下，拖拽动画降级为即时位移，不做回弹或惯性。

## 6. 日期滚轮

```text
             选择灵感发生的日期

          年              月              日
       2025             07              16
     > 2026 <         > 08 <          > 17 <
       2027             09              18

       2026 年 8 月 17 日 · 当天有 4 条灵感
        [今天] [最近有灵感]          [查看白板]
```

设计规则：

- 中间选中行高对比，旁边行用渐隐；
- 滚动结束后吸附；
- 只有用户停止滚动后才预取白板，避免频繁请求；
- 月份变化导致日期无效时，自动收敛到当月最后一天并用轻提示说明；
- 鼠标滚轮只影响指针所在列；触摸拖动具备惯性但不过度；
- 提供日期输入的无障碍替代入口。

## 7. 日白板

### 7.1 布局模型

白板不是无限画布，而是“有限自由布局”：

- 桌面端使用 12 列逻辑网格；
- 每张贴片占 3–5 列；
- 由时间、类型和内容长度确定初始位置；
- 用户移动后保存 `x/y/z/rotation`；
- “整理白板”重新计算，但先提供预览/撤销。

### 7.2 贴片层级

```text
┌──────────────────────────┐
│ 09:42   [产品想法]   ○选择 │
│                          │
│ “如果按日期回看灵感，     │
│  会不会更像回到当天？”    │
│                          │
│ 🖼 2   🎙 01:24   AI标签  │
└──────────────────────────┘
```

状态表达：

- 默认：纸片；
- hover/键盘焦点：轻微抬升；
- 选中：清晰描边、勾选标记；
- 处理中：附件区域显示局部骨架；
- 处理失败：附件上显示可重试，不把整卡标红；
- 已引用：底部显示“用于 2 个项目”。

### 7.3 多选进入创作

选中第一张贴片后，底部操作栏出现；选择多个时不弹出遮挡白板的模态框。点击“用这些灵感创作”后进入素材篮，仍可返回保持选择。

## 8. 热点雷达

### 8.1 视觉结构

```text
┌──────────────┬─────────────────────────────────────────────┐
│ 热点来源      │ AI 热点                 已更新 3 分钟前 刷新 │
│ ● AI 热点     │                                             │
│ ＋后续扩展    │ #1 标题                    新鲜  来源/时间    │
│              │ 摘要……                     [详情][加入创作]  │
│              │                                             │
│              │ #2 标题                    变旧  来源/时间    │
└──────────────┴─────────────────────────────────────────────┘
```

雷达感来自细微的扫描光晕和时间状态，不需要真的做雷达圆盘。信息可读性优先。

### 8.2 可信度表达

- 明确显示“来源”和“抓取于”；
- AI 摘要加 `AI 摘要` 标签；
- 原标题与 AI 改写标题分开；
- 详情页提供打开原来源；
- 无法获取详情时保留列表快照并标注；
- 多来源聚类后显示来源数量，不伪装为单一权威结论。

## 9. 选题与创作工作台

避免传统聊天框成为主界面。建议使用结构化卡片：

```text
左：素材与证据          中：当前阶段                  右：AI 建议
灵感 / 热点 / 原图      候选选题 / 创作要点 / 平台稿   相关性依据
Evidence 来源           可直接编辑和锁定              风险与缺口
素材权限与处理状态      版本与同步状态                局部生成操作
```

共享阶段条：

```text
素材装配 → 选题确认 → 创作要点（核心内容骨架）
```

确认创作要点后，阶段条按选择的平台展开：

```text
小红书：笔记策略 → 标题/钩子 → 图文分镜 → 卡片/正文 → 预览 → 导出（P1/Phase 4 可实验性发布）
公众号：文章策略 → 标题 → 文章大纲 → 分节成稿 → 全文编辑 → 排版 → 草稿箱
```

“创作要点”页面以 AI 预填卡片呈现主题、读者问题、核心主张、关键观点、Evidence、案例、可用素材、待核验项和内容禁区；不要求用户面对空白表单，也不生成平台中性的完整长文。两个平台分支可并行存在，但各自独立保存。

阶段不是不可逆向导：用户可回到共享阶段；当 `ContentCore` 已更新，平台稿显示“核心内容有更新”与差异入口。重新生成时创建新版本，不静默覆盖平台人工编辑。

## 10. 小红书原生创作体验

Code-2 的模板、密度、3:4 预览可保留，但它应定位为小红书原生分镜工作区，而不是“把长文适配成图片”。推荐交互顺序：

1. 基于 `ContentCore` 选择笔记策略（知识、故事、清单、观点、案例等）；
2. 确认标题候选与开头钩子；
3. 先做图文分镜，明确每页的叙事目的；
4. 为每页装配原图、信息卡、引用或可选概念图；
5. 编辑卡片文案，再补充短正文和话题；
6. 逐页预览、锁定、校验并导出。

工作区布局：

- 左侧为页序、页面角色和卡型；
- 中间为 3:4 实时预览；
- 右侧为当前页文本、图片槽、Evidence 来源和布局设置；
- 允许逐页使用不同卡型，但保持统一主题 token；
- 拖动原图到素材槽，并显示裁切安全区；
- 溢出、低分辨率、缺图均基于真实测量；
- “重新生成这一页”不能影响其他已锁定页面；
- 分镜引用 `ContentCore` 的事实与素材，但标题、顺序和表达可完全平台化。

卡型建议：封面、问题、观点、步骤、对比、清单、案例、引用、总结/CTA、原图叙事。禁止默认把公众号 H2 或完整长文段落机械切成卡片。

## 11. 公众号原生创作与排版体验

公众号分支先完成文章创作，再进入排版：

```text
文章策略 → 标题候选 → 文章大纲 → 分节成稿 → 全文编辑 → 排版预览
```

- 文章大纲只属于公众号分支，不作为两个平台共同结构；
- 支持按章节生成、排序、锁定和局部改写；
- 中间显示接近微信的阅读宽度和完整文章；
- 左侧显示章节结构与 Evidence；
- 右侧显示主题、字号、间距、图片、引用样式和合规提示；
- 用户编辑的是公众号结构化稿或 Markdown，不直接维护内联 HTML；
- 排版服务将公众号稿转换为微信兼容的内联 HTML；
- 支持“复制到公众号”和“推送草稿箱”；
- 主题切换不改变文章语义结构；
- 显示外链、图片上传和不兼容样式的转换结果。

可借鉴或私有部署 [doocs/md](https://github.com/doocs/md) 的 Markdown 即时渲染、主题和图片上传能力，也可将兼容服务封装为 API。工作台应保存自己的公众号稿和版本信息，不把第三方编辑器内部状态当作唯一内容事实源。

## 12. 响应式策略

### Web MVP

- ≥1280px：三栏创作工作台；
- 768–1279px：素材或 AI 建议折叠为抽屉；
- <768px：记录、查看、轻编辑可用；复杂卡片排版引导到桌面。

### 移动端后续

- 首页保留两个主按钮；
- 快速拍照、直接录音；
- 白板默认转为纵向贴片流，提供缩略地图；
- 深度创作采用阶段卡片，而非压缩桌面三栏。

## 13. 可用性验收重点

- 新用户无需教程可在 30 秒内记录一条带图灵感；
- 用户能明确区分“原始素材”“AI 摘要”“生成内容”；
- 用户可在 1 分钟内找到某天并选择两条灵感；
- 低相关性提示不阻止继续创作；
- 用户能理解“核心内容骨架”与两份平台稿的关系，并知道平台稿不会被静默覆盖；
- 发布前能清楚看到将发布的标题、正文、图片顺序和平台账号；
- 所有主操作可由键盘完成。

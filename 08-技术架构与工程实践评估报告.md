# 技术架构与工程实践评估报告

> 评估对象：Content-Collect-Create/ 下 CodeX 生成的 8 份方案文档  
> 评估维度：架构设计、模块解耦、可扩展性、技术选型、工程落地实践  
> 评估立场：仅做批判性评估与调整建议，不修改原方案文档  
> 日期：2026-08-18

---

## 1. 评估前提与已做假设

本评估基于以下假设进行，若这些假设不成立，部分结论需要重新校准：

1. **目标用户假设**：首版面向同时运营小红书 + 公众号的个人创作者或 1–3 人小团队，而非矩阵号批量工具。
2. **平台约束假设**：小红书无官方公开发布 API，公众号有官方草稿箱 API；两条通道的合规水位差异显著。
3. **资源假设**：当前团队可维护一套 Web 全栈 + 本地桥方案，但不应同时维护多语言后端。
4. **Code-2 现状假设**：Code-2 已有 React/TS 卡片渲染能力，但缺少选题、母稿、平台适配的上游环节。
5. **AI 能力假设**：LLM 调用按量付费，需有成本与延迟控制；ASR/OCR/图片理解依赖第三方服务。

---

## 2. 总体判断

CodeX 产出的方案在产品层面**方向基本正确**，尤其是：

- 坚持“人在回路”和分阶段确认（选题 → 大纲 → 母稿 → 平台适配）；
- 采用“单一母稿 + 双平台编译器”的内容架构；
- 把小红书发布定位为“本地实验性桥”，公众号走官方草稿箱 API；
- 提出 Evidence Pack、块级 AST、来源追踪等可追溯设计。

但在**技术架构的完整性与工程落地的可行性**上，仍存在明显缺口。核心问题可归纳为一句话：

> **产品定义完整度高于工程实现的可执行度。**

具体表现为：后端技术栈未敲定、AI Gateway 欠具体、MVP 范围偏大、数据模型过于提前泛化、缺少成本与运维控制、排期乐观。以下分维度展开。

---

## 3. 架构设计评估

### 3.1 分层架构总体合理，但缺少关键决策

`06-系统架构数据模型与研发计划.md` 提出的分层是合理的：

```
Web App → Application API → DB / Object Storage / Job Queue → Workers → AI Gateway / ASR / OCR / Hotspot Adapters
                              ↓
                    Publication Service → WeChat Connector / Local Bridge → xiaohongshu-mcp
```

这条链路覆盖了内容状态、文件、异步任务、AI 编排、发布通道，符合事件驱动 + 状态持久化的基本盘。

**需要调整的地方：**

| 问题 | 理由 | 建议 |
|---|---|---|
| **未确定后端语言/框架** | 文档只说“REST 或类型安全 RPC，首版不要同时维护过多协议”，但 Node/TS、Python、Go、Rust 的成本模型完全不同。这会影响 ORM、队列、部署、类型共享、Code-2 复用度。 | 在 Phase 0 必须做出选择。考虑到 Code-2 是 React/TS，若团队前端能力强，可优先考虑 **Node/TS 全栈**（如 Next.js / Nitro / Hono + Postgres）；若重视 AI 管道与异步任务，可考虑 **Python 后端 + TS 前端**。不要拖到编码开始再决策。 |
| **AI Gateway 只停留在概念层** | 文档多次提到“统一 AI Gateway”，但未说明它是内部库、独立服务、还是供应商抽象层。 | 明确 Gateway 的职责边界：密钥管理、模型路由、重试降级、成本计量、Schema 校验、Prompt 版本。建议首版做成服务端内部模块，而非独立服务。 |
| **队列/任务系统未选型** | “队列 + Worker” 过于模糊。BullMQ、Celery、Temporal、Inngest、自研轮询表差异巨大。 | 根据后端语言选型同步确定。Node 生态可用 BullMQ + Redis；Python 可用 Celery / RQ；若希望可视化状态机，可评估 Temporal。 |
| **实时通信策略偏弱** | 文档写“轮询起步，P1 再接 SSE/WebSocket”。对于 ASR/OCR/LLM 生成等长任务，轮询会放大前端复杂度和延迟。 | 首版可直接用 **SSE** 推送任务状态，实现成本低、协议轻，避免后期大面积改造。 |

### 3.2 本地发布桥的安全模型需要更严谨

`05-双平台排版与发布技术方案.md` 中本地发布桥的时序图是合理的：云端只创建带签名的短期任务，本地桥持有 Cookie 并执行浏览器自动化。

但缺失以下关键设计：

1. **任务认领与签名的具体机制**：短期签名是 JWT？HMAC？有效期多久？如何防止本地桥被复用到其他账号？
2. **本地桥的权限边界**：本地桥需要哪些系统权限？是否必须管理员权限？如何更新？
3. **回传数据的最小化原则**：除了成功/失败状态、外部链接、错误码，是否还需要回传截图？截图可能包含 Cookie、账号信息等敏感内容，应默认本地保存、用户选择上传。
4. **防重放与幂等**：文档提到 `publication_intent_id`，但未说明本地桥如何校验 intent 与 payload 哈希的绑定关系。
5. **MCP Server 的生命周期**：`xiaohongshu-mcp` 需要长期运行还是按需启动？端口冲突、Cookie 刷新、Session 独占（同一账号不能多处登录）如何处理？

**建议补充**：在 Phase 0 输出一份《本地发布桥威胁模型文档》，明确信任边界、数据流、最小权限、失败恢复。

### 3.3 数据模型过早泛化，MVP 承载过重

`06-系统架构数据模型与研发计划.md` 中定义了 workspaces、workspace_members、platform_connections、inspirations、assets、asset_derivatives、inspiration_types、daily_boards、hotspot_sources、hotspot_snapshots、hotspot_items、creation_projects、project_sources、topic_briefs、outlines、content_drafts、storyboards、rendered_assets、publication_packages、publication_jobs 等近 20 张表/实体。

**问题：**

- 这是中后期的完整数据模型，不是 MVP 数据模型。过早泛化会拖慢首版迭代。
- 例如 `workspaces` + `workspace_members` 的 RBAC 在 MVP 阶段可能只有一个用户，但却要在每张表上维护 `workspace_id`。
- `daily_boards` 与 `inspirations` 的关系、版本化 (`version`) 字段在多表上的同步策略未说明。
- `content_drafts` 同时存 `canonical` 和 `platform` 两种 draft，加上 `source_draft_id` 自引用，若版本管理不慎易出现“母稿改了但平台稿不知道要不要覆盖”的复杂合并问题。

**建议：**

1. 区分 **MVP 数据模型** 与 **长期数据模型**。MVP 首版可合并部分实体，例如：
   - `topic_briefs` + `outlines` + `content_drafts(canonical)` 可统一为 `projects.content_json` 的多个版本；
   - `workspace_members` 可延迟到 P1；
   - `daily_boards` 在 MVP 可由 `inspirations` 按日期聚合视图替代，不必单独持久化布局版本。
2. 明确每张表的写入 QPS、数据量级、保留策略，避免为用例图完整而堆砌表。

---

## 4. 模块解耦评估

### 4.1 平台编译器与母稿的解耦是亮点

“Canonical Draft → XHS Storyboard Compiler / WeChat Article Compiler” 的设计是方案中最坚实的架构决策之一。它把内容语义层与平台呈现层分开，符合编译器思维。

**值得保留：**

- AST 块级结构作为内部事实源；
- Markdown 仅作为导入/导出格式；
- 平台稿保存 `source_draft_version_id`、`compiler_version`、`user_locked_paths`；
- 母稿变更时不静默覆盖平台稿，而是生成新版本 + 差异预览。

### 4.2 Skill 概念边界需要收紧

文档中 Skill 同时承担了：

1. 热点源适配器（AIHot Skill）；
2. 未来可扩展的“热点源市场”；
3. 公众号排版可能引入的 skill；
4. 自定义 Skill 的潜力。

**问题：**

- 将“Skill”与“热点源适配器”混为一谈，容易让人误以为 Skill 是一种通用插件运行时。实际上当前方案中的 `HotspotAdapter` 更接近普通的服务端适配器，没必要包装成“Skill”这种 overloaded 概念。
- `04-内容创作与多模态AI方案.md` 提到“不要允许用户上传任意代码成为 Skill”，说明作者已意识到风险，但没有给出自定义 Skill 的安全模型（沙箱、签名、权限、审计）。
- 如果未来要让用户自定义热点源，更安全的做法是**声明式连接器**（URL、method、headers、字段映射）+ 服务端沙箱执行，而不是开放代码上传。

**建议：**

- MVP 阶段取消“Skill 市场”概念，统一称为 **热点源适配器 (Hotspot Adapter)**。
- 自定义来源放在 P2，并配套设计声明式连接器 + 字段映射 + 响应体大小限制 + 超时熔断。
- 若必须引入“Skill”术语，先给出严格的定义：它在本产品中仅指“受限于域名白名单、输出 Schema 固定、由服务端调用的数据适配器”。

### 4.3 Evidence Pack 与创作流水线的接口需要定型

Evidence Pack 的概念很好：一次创作使用的素材清单，包含 inspirations、hotspots、account_profile、constraints。

**问题：**

- 文档没有明确 Evidence Pack 是**内存中的调用上下文**还是**持久化实体**。如果是持久化的，需要独立表；如果是临时的，则不应过度设计。
- 模型输出引用 Evidence ID 的机制（`evidence_ids`）在 AST 块上如何实现？是 LLM 自己生成引用，还是后处理阶段匹配？这直接影响 prompt 设计和评估指标。
- 若 Evidence Pack 作为 LLM 输入，其序列化格式、上下文预算裁剪策略、图片是否上传原图等，需要更具体的契约。

**建议：**

- 明确 Evidence Pack 是“一次创作的调用上下文”，不单独持久化，而是保存在 `project_sources` + 项目快照中。
- 设计 `EvidenceRenderer`，负责把 inspirations / hotspots / assets 渲染成 LLM 可消费的文本/图片序列。
- 对“引用 Evidence ID”设计评估脚本，离线跑真实项目，检查 LLM 是否经常 hallucinate 引用。

---

## 5. 可扩展性评估

### 5.1 平台扩展性设计较好

`PublicationAdapter` 接口和 `Platform Strategy` 分层，为未来接入其他平台（如抖音图文、知乎、即刻）留下了清晰扩展点。

`HotspotAdapter` 接口也提供了可扩展的抽象。

### 5.2 但部分扩展点被提前引入

以下扩展性设计在 MVP 阶段并非必要，反而增加复杂度：

| 扩展设计 | 问题 | 建议 |
|---|---|---|
| **多工作区 (workspaces)** | MVP 几乎确定单用户/单团队，多工作区会带来权限、隔离、邀请流程等额外工程。 | 延迟到 P1 或 P2。 |
| **团队协作角色 (owner/editor/viewer)** | 同上，MVP 阶段单人使用即可。 | 数据库预留字段可以，但不必实现功能。 |
| **多主题与视觉回归测试矩阵** | 视觉回归测试需要基础设施（CI、基线图片存储、diff 工具），首版若只提供 2–3 套主题，测试矩阵可大幅缩小。 | 明确首版主题数量（建议 2 套），视觉回归先做关键路径，P1 再扩展矩阵。 |
| **灵感星云探索视图** | 图三式的语义星云是产品愿景，但实现成本高，且与日期白板存在主导航竞争。 | 明确列为 P2，不在 MVP 做任何基础设施预留。 |
| **自定义 Skill / 热点源市场** | 安全风险高，且 MVP 只有 AIHot 一个来源。 | 延迟到 P2，先做好声明式连接器设计。 |

### 5.3 内容类型的可扩展性考虑不足

当前灵感类型是文字、图片、音频，未来会扩展移动端拍照/录音。但文档对以下问题考虑较少：

- 视频灵感是否纳入？小红书和公众号都支持视频，若未来要支持，当前 `assets` 表的 `kind: image/audio/file` 会显得不足。
- 链接/网页剪藏是否是一种灵感类型？它对应的是抓取 + 解析 + 快照，与图片理解的工程路径不同。
- 文件类型（PDF、Word）的存储与检索策略未提及。

**建议：** 将 `assets.kind` 设计为更开放的枚举或 tag 体系，并在 `asset_derivatives` 中支持视频关键帧、网页快照、PDF 文本等派生类型。

---

## 6. 技术选型评估

### 6.1 Web 前端：选型合理，但需明确是否复用 Code-2

文档建议沿用 React + TypeScript + Tailwind + TanStack Router/Start，这与 Code-2 技术栈一致。

**需要明确的问题：**

- Code-2 当前是“项目工作台 + 单页面大组件”，新工作台要新增大量路由（inspiration、hotspots、creation、publisher）。是直接重构 Code-2，还是新建一个应用、复用组件？
- 若复用 Code-2 的 TanStack Start，需要确认其版本成熟度、部署方式、是否支持所需的 SSR/SSG 策略。
- 母稿编辑器、小红书卡片编辑器、公众号预览器，这三个重度交互组件不宜全部塞进同一个路由包。需要按功能拆包（code splitting）。

**建议：**

- 采用 **Monorepo 结构**：`apps/web`（新工作台）、`packages/content-model`（AST/类型）、`packages/xhs-renderer`（Code-2 抽出的渲染器）、`packages/wechat-compiler`。
- Code-2 的现有页面作为旧入口保留，逐步迁移 renderer 和 card model，而不是一次性重写全部 UI。

### 6.2 后端：选型缺失是最大的架构风险

如 3.1 节所述，后端语言/框架未选定。这里进一步展开不同选择的权衡：

| 方案 | 优势 | 劣势 | 适合情境 |
|---|---|---|---|
| **Node/TS 全栈 (Next.js App Router / Nitro / Hono)** | 与前端类型共享；可复用 Code-2 类型和工具函数；部署简单。 | 异步任务、AI 管道、队列生态弱于 Python；长任务占用主进程需小心。 | 团队前端强、首版验证产品方向。 |
| **Python 后端 (FastAPI/Django + Celery)** | AI/ML 生态成熟；异步任务、文件处理、OCR/ASR 集成顺畅。 | 与前端类型不同源；需要维护两套类型定义；团队学习成本。 | 团队有 Python 基础、重视 AI 管道。 |
| **混合：Node 负责 API/Web，Python 负责 AI Worker** | 前后端类型共享 + AI 生态成熟。 | 运维复杂度最高；跨服务通信、序列化、错误处理增加成本。 | 有充足运维资源的中长期方案。 |

**建议：**

- 若团队当前以 TS 为主且希望快速验证，选择 **Node/TS 全栈**，并用 **BullMQ + Redis** 处理异步任务。AI Worker 可用独立 Node 进程或轻量 Python 微服务（仅在必要时）。
- 若团队后续要大量做模型微调、评估、RAG，再考虑 Python 后端。
- **不要同时启动两套完整后端**。

### 6.3 数据库：Postgres/Supabase 合理，但 Supabase 有锁定风险

Postgres 是安全选择。但“Supabase 兼容”若指使用 Supabase 托管服务，需要考虑：

- Supabase 的 Row Level Security (RLS) 是否需要？MVP 阶段可能过度设计。
- Supabase Storage 与自定义 S3/R2 的关系。
- Supabase Auth 是否与自定义账号体系冲突。
- 若未来需要迁移出 Supabase，成本如何。

**建议：**

- 首版使用 **自管 Postgres + 自管对象存储（或 R2）**，通过 ORM（Prisma / Drizzle）管理迁移。Supabase 可作为 P1 快速上手的选项，但不作为架构依赖。

### 6.4 AI 服务：未明确供应商，需要补全

文档提到 ASR、OCR、图片理解、Embedding、LLM、图像生成，但没有给出具体供应商选项。MVP 阶段至少需要：

- **LLM**：建议多模型策略——便宜模型做分类/标签/摘要，强模型做选题/大纲/改写。可选 OpenAI / Claude / 通义千问 / DeepSeek / Kimi 等。
- **ASR**：Whisper API、讯飞、阿里云等。
- **OCR + 图片理解**：Qwen-VL、GPT-4o、Claude 3.5 Sonnet、百度/腾讯/阿里云 OCR。
- **Embedding**：text-embedding-3 / bge / 通义等。
- **图像生成**：DALL·E、Midjourney API、即梦、可灵等，但 MVP 不应默认调用。

**需要补充的设计：**

- 成本预算与告警；
- 模型降级策略（强模型不可用时是否用弱模型兜底）；
- Prompt 版本管理；
- LLM 输出 Schema 校验与修复；
- 图片上传给 LLM 的隐私开关。

### 6.5 小红书发布桥：技术选型正确，但需本地化验证

`xiaohongshu-mcp` 是目前社区可用的非官方方案，选择它是合理的。但文档中对其限制描述较少，需要补充：

- 该 MCP Server 是 Streamable HTTP + JSON-RPC 2.0，客户端需要 `streamable_http_client`；
- Cookie 落盘在 exe 同级目录，服务必须从该目录启动；
- 首次发布可能 >120s，客户端超时需设 600s；
- 同一账号不能多处网页登录，否则 MCP 登录态被踢。

这些来自项目工作记忆的事实，应在方案中明确记录。

---

## 7. 工程落地实践调整建议

### 7.1 MVP 范围需要大幅收敛

当前 Phase 1–4 的排期（约 9–13 周）要完成：首页、记录、存储、ASR/OCR、日期滚轮、白板、AIHot、素材篮、相关性、选题、大纲、母稿编辑器、小红书编译器、公众号编译器、微信草稿箱、本地发布桥、灰度。

**这是典型的“MVP 膨胀”风险。** 即便功能都能写出来，也不代表体验能闭环。

**建议收缩为两个更小的里程碑：**

**Milestone 1：灵感记录 + 日期白板 + 热点查看（4–5 周）**

- 首页、快速记录（文字/图片/音频上传）、ASR/OCR 异步、日期滚轮、单日白板、灵感多选、AIHot 列表/缓存/详情。
- 不进入创作、不生成内容、不排版、不发布。
- 验证核心假设：用户是否愿意按日期记录和回顾灵感？热点查看是否有价值？

**Milestone 2：从灵感到平台稿的端到端单平台通路（6–8 周）**

- 选择公众号作为首条通路（官方 API、风险低、可闭环）。
- 素材篮 → 选题 → 大纲 → 母稿 → 公众号 HTML → 草稿箱。
- 小红书先只做“导出 PNG/ZIP 包”，不接入 MCP。
- 验证核心假设：母稿模式是否被用户接受？公众号草稿箱通路是否稳定？

**Milestone 3：小红书编译器 + 本地发布桥（再 6–8 周）**

- 在公众号通路稳定后，再投入小红书编译器和发布桥。
- 此时已有母稿 AST 和平台编译器经验，风险更可控。

### 7.2 状态机与版本控制需要更具体的实现策略

`02-产品需求与信息架构方案.md` 中定义了 11 个状态的项目状态机。状态机是好设计，但需要补充：

- **状态转换的事件命名与触发方**：哪些由用户触发？哪些由任务完成触发？哪些由错误触发？
- **幂等性**：同一任务被 Worker 重试时，如何避免状态跳变？
- **并发控制**：用户在 A 浏览器生成大纲，同时在 B 浏览器编辑母稿，如何处理冲突？
- **版本命名规则**：版本是整数递增、语义化版本、还是时间戳？`content_drafts` 多版本如何清理旧版本？
- **乐观锁还是悲观锁**：建议对 `projects` 行使用 `version` 字段做乐观锁。

### 7.3 异步任务系统需要更具体的失败处理

文档列出了 12 种任务类型，但未说明：

- 任务的最终一致性如何保证（at-least-once / exactly-once）？
- 任务输入/输出体积限制（例如 LLM 输出可能很长）。
- 任务失败后的死信队列（DLQ）策略。
- 用户取消任务的机制。
- 任务进度的粒度（百分比还是阶段名）。

**建议：**

- 所有生成任务统一设计为 `idempotent_key + status + result_ref` 模式；
- 结果较大时存对象存储，数据库只存 `result_uri`；
- 失败任务进入可人工查看的“失败中心”，而不是自动无限重试。

### 7.4 发布幂等与防重复需要落地细节

发布是高风险动作，文档虽然强调了幂等，但缺少具体机制。

**建议实现：**

1. `publication_intent_id` 由服务端在用户点击确认时生成；
2. `intent` 包含内容哈希、标题、图片哈希、创建时间、目标平台账号；
3. 本地桥在执行前向服务端查询该 `intent_id` 是否已有成功回执；
4. 若本地桥长时间无响应，状态置为 `uncertain`，必须人工介入；
5. 微信草稿箱同样使用 `intent_id` 去重，避免重复创建草稿。

### 7.5 缺少 CI/CD、部署与可观测性设计

整套方案没有提到：

- 如何部署 Web、API、Worker？
- 数据库迁移如何执行？
- 本地发布桥如何分发和更新？
- 如何监控 LLM 成本、任务失败率、发布成功率？
- 日志收集、告警、Tracing 策略是什么？

**建议补充：**

- 部署：Vercel / Railway / Fly.io / 自有服务器，根据后端选型确定；
- 迁移：使用 ORM 迁移工具（Prisma Migrate / Alembic / Atlas），部署时自动执行；
- 本地桥：提供安装脚本或桌面封装（如 Tauri），并考虑自动更新机制；
- 可观测：至少记录关键指标（灵感创建数、任务成功率、发布成功率、LLM 调用成本、平均延迟）。

### 7.6 视觉回归测试需要基础设施

文档提出视觉回归测试要覆盖“每种主题×卡型×不同长度文本×缺图/低清/超长标题×中英 emoji”，这是正确的方向，但没有说明如何落地。

**建议：**

- 使用 Playwright + pixelmatch 或 Chromatic；
- 视觉回归先做“导出 PNG 与预览一致”这一条核心路径；
- 不要试图在 MVP 覆盖全部组合，先建立流程再扩展矩阵。

### 7.7 成本与速率限制需要前置设计

AI 调用（ASR、OCR、LLM、图片理解）是主要变动成本。MVP 若没有成本监控，很容易出现单用户异常请求导致费用飙升。

**建议：**

- 对每个用户/工作区设置每日 LLM token 上限和图片理解次数上限；
- 对热点刷新设置缓存时间和全局速率限制；
- 记录每次 AI 调用的 cost，便于后续定价和优化。

---

## 8. 风险再排序与关键决策清单

### 8.1 风险等级调整

| 风险 | 原方案等级 | 评估后等级 | 理由 |
|---|---:|---:|---|
| 小红书第三方通路失效或账号风险 | 高 | **高** | 合理，但应增加“本地桥更新与分发”子风险。 |
| 微信权限/字段变化 | 高 | **中** | 官方 API 相对稳定，字段变化可通过 capability probe 吸收。 |
| AIHot 服务不稳定或条款不清 | 中高 | **中** | 热点不是 MVP 核心路径，可降级为手动录入退路。 |
| AI 生成事实错误 | 高 | **高** | 需要更具体的 Evidence 引用评估机制。 |
| 图片版权/隐私 | 高 | **高** | 合理，建议增加 EXIF 清理和原图授权字段的自动化检查。 |
| 过度复杂导致创作摩擦 | 高 | **高** | MVP 范围必须收缩，否则此风险会变成现实。 |
| Code-2 重构范围失控 | 中 | **高** | 抽 renderer 和 model 容易，但重构全部 UI 极易失控。 |
| 任务重复执行 | 高 | **高** | 需要更具体的幂等实现方案。 |
| 后端技术栈未确定 | 未列出 | **高** | 这是当前最大的架构风险，会阻塞后续所有工程决策。 |
| LLM 成本失控 | 未列出 | **中高** | MVP 阶段必须设置预算和上限。 |

### 8.2 Phase 0 必须做出的关键决策

在写第一行代码前，建议明确以下决策并写入文档：

1. **后端语言/框架**（Node/TS vs Python vs 混合）。
2. **数据库与对象存储供应商**（自管 Postgres + R2 vs Supabase）。
3. **异步任务系统**（BullMQ / Celery / Temporal / 其他）。
4. **AI 供应商组合**（LLM、ASR、OCR、图片理解、Embedding）。
5. **Code-2 复用策略**（Monorepo 抽包 vs 在旧页面改造）。
6. **本地发布桥的分发与更新机制**。
7. **MVP 最终功能范围**（建议按 7.1 节收缩为两个 Milestone）。
8. **部署目标平台**（Vercel / Railway / Fly.io / 自有服务器）。

---

## 9. 结论

CodeX 生成的方案在产品定义层面已经相当完整，尤其对“人在回路”“母稿 + 编译器”“可追溯 Evidence”“发布风险隔离”等关键原则的把握是到位的。

但在**工程可执行性**上，当前方案存在以下必须调整的核心问题：

1. **后端技术栈缺失**——这是最大阻塞点，会级联影响数据模型、异步任务、部署、类型共享等所有决策。
2. **MVP 范围过大**——当前排期试图在 9–13 周内完成从灵感到双平台发布的全链路，失败概率很高。建议拆分为“灵感记录 → 公众号单通路 → 小红书编译器 + 发布桥”三个里程碑。
3. **数据模型过早泛化**——近 20 个实体、多工作区、团队协作等应延迟到 P1/P2。
4. **Skill 概念边界模糊**——MVP 应聚焦“热点源适配器”，把 Skill 市场和自定义 Skill 放到 P2。
5. **AI Gateway、本地桥安全模型、异步任务失败处理、成本监控** 等关键工程细节需要补充。
6. **缺少 CI/CD、部署、可观测性、本地桥分发** 等运维设计。

### 建议的下一步动作

1. **召开 Phase 0 架构决策会**，确定后端语言、数据库、队列、AI 供应商、部署目标。
2. **重写一份《MVP 技术规格书》**，把当前 8 份文档中的产品定义收敛为可执行的接口、数据模型、任务类型、错误码。
3. **先做 Milestone 1 原型**（记录 + 日期白板 + 热点查看），用真实用户访谈验证核心假设。
4. **在 Milestone 1 完成后，再启动公众号通路**，积累母稿编辑器和 AST 编译器经验。
5. **小红书 MCP 通路放在 Milestone 3**，并在此之前完成本地发布桥的威胁模型和分发方案。

---

*本报告为技术架构与工程实践角度的评估意见，不构成对产品方向的否定。产品方向成立，但落地节奏和工程细节需要收紧。*

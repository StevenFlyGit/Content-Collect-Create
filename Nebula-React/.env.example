# 前端本地配置示例（复制为 .env.local；*.local 已被 Git 忽略）
# 使用 Vite 本地代理时保持 /api；若前后端分开部署，填写后端公开地址并包含 /api。
VITE_API_BASE=/api
# 必须与服务端 WORKSPACE_ID 使用同一个有效 UUID；当前仅用于开发期工作区隔离，不等同于正式登录鉴权。
VITE_WORKSPACE_ID=00000000-0000-0000-0000-000000000001
